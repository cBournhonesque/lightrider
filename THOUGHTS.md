---
date: 2026-05-22
topic: "Lightrider project design, implementation plan, and agent notes"
tags: [lightrider, bevy, lightyear, game-design, multiplayer]
status: active
---

# Lightrider Thoughts

This is the durable project document for Lightrider. Keep it up to date whenever code, architecture, tooling, gameplay rules, deployment assumptions, debugging knowledge, or operational findings change.

## Game Design Document

### Product Goal

Lightrider is a Rust, Bevy, and Lightyear clone of Powerline.io. The first milestone is a fully working online multiplayer prototype with intentionally barebones graphics. Cosmetics such as animation polish, sound, music, textures, fonts, richer colors, and original asset extraction come after gameplay and networking hold up online.

Use `/spare/ssd/cbournhonesque/src/other/snakegame/original/powerline.io` as inspiration for behavior, tuning, UI ideas, and later assets. The prototype should prioritize readable primitives and correctness over visual fidelity.

### Core Gameplay

- Players control a continuously moving light snake on an orthogonal grid-like plane.
- A snake is a head plus a tail polyline. Direction changes add turn points; the tail follows the head path and is shortened from the back to maintain length.
- Players steer up, down, left, and right. A 180-degree reversal is invalid.
- Hitting another snake, hitting yourself, or hitting the arena boundary kills the snake.
- Food spawns as very small static dots. A snake eats food when its post-movement head position is inside the configured pickup radius; pull-in is a client-side confirmed pickup animation, not authoritative food movement.
- Default food density is intentionally higher than the early prototype: the default config targets 400 food entities per room and spawns toward that target every 0.05 seconds.
- Eating food grows the snake, increases score, and gives a short smooth acceleration burst.
- Score starts at 0 and counts growth beyond the initial snake length. Starting tail length is not part of score.
- When a snake dies, the server drops food samples along the killed snake trajectory.
- The Powerline signature mechanic is proximity speed boost: a snake accelerates when moving close and parallel enough to another snake trail, then decelerates back toward base speed when not close.
- The server remains authoritative for deaths, kill reasons, food pickups, score, room assignment, and respawn cooldowns.
- Bots should use the same movement, collision, food, scoring, death, and respawn rules as players.

### Prototype Rendering

- Rendering is data-driven through `render.use_assets`. When true, Bevy sprites use the copied Powerline `assets/powerline/sheet.png`; when false, the client keeps the earlier gizmo/debug primitives.
- Current asset-backed rendering uses a dark-blue arena interior, a subtle procedural grid background, a uniform blue out-of-bounds clear color, a thin bright-blue arena border with a soft low-alpha glow, thin HDR snake core meshes, smaller round white snake heads with very soft colored glow behind them, and tinted food sprites. It intentionally keeps behavior and interpolation unchanged.
- The background is procedural rather than a tiled PNG, which avoids the black seams that appeared between repeated background tiles. Bloom is thresholded so the HDR snake cores and arena edge can glow without blurring the whole map/background.
- Proximity boost has local visual feedback: the client uses the same ray-vs-tail geometry as movement boost detection, filters for parallel contacted trails, renders lightning to the contacted trail core as soon as there is space, and shows the original Powerline spark marker at that same contacted core point when speed is high or the gap is too tight for lightning. High-speed snakes emit larger original `particleDot` sprites behind the head.
- The leader has the original crown sprite above their head, and the minimap shows both the leader dot and a small crown marker. The minimap has a visible UI border so it can be distinguished from the game background.
- A semi-transparent Bevy HUD `DEBUG` button toggles local network stats, including client ping/RTT and jitter from Lightyear link stats.
- Sound uses sliced clips from the original `sounds/out.ogg` sprite: confirmed deaths play the crash sound, confirmed food pickups play the grab sound, and speed loops use the original line-loop clips with volume driven by speed. Local sounds play at local volume; other players' death, food, and speed sounds use their snake-head location with configurable distance attenuation and Bevy spatial panning.
- Browser audio still depends on the browser allowing playback after a user gesture. The web join/menu interaction should provide that gesture; if sound is silent in-browser, first check browser autoplay/mute state before changing Bevy audio code.
- Death sounds must only be triggered from server-confirmed `PlayerDeath` messages. Predicted/rollback state should not emit one-shot sounds, and looping sounds should reuse existing audio entities instead of restarting during rollback or correction.
- Confirmed death visuals should be subtle: the killed tail flashes white and fades in-place quickly. It should not collapse toward its midpoint or move away from the actual path.
- The normal camera should be close to the local head and grow outward with snake size. Debug mode can toggle to a very zoomed-out camera and show shortcuts with `?`.
- Later cosmetic backlog: better per-player colors, glow, sparks on proximity boost, richer death effects, food pickup animation polish, logo/menu assets, minimap/leaderboard styling.

### Arena And Rules

- Original Powerline client constants of interest:
  - `GAME_SCALE = 10.0`
  - `UPDATE_EVERY_N_TICKS = 3`
  - `INTERP_TIME = (1000 / 30) * UPDATE_EVERY_N_TICKS`
  - Default client-side arena values include `arenaWidth = 5000.0`, `arenaHeight = 1600.0`, centered at `(0.0, 0.0)`.
  - Kill reasons include left screen, killed, boundary, and suicide.
- Current default config uses a Powerline-like `5000.0 x 1600.0` arena. `config/test.ron` uses an `800.0 x 600.0` arena for quick local testing.
- Initial target room limit is 50 players per room, matching the intended scale of the original Powerline.io.
- Exact original behavior is not required when the original uses ad-hoc logic, but defaults should produce a similar feel.

### Configuration

- Use typed, data-driven config loaded from RON.
- Keep one `config/default.ron` tuned toward Powerline-like behavior.
- Keep smaller test configs such as `config/test.ron` for local collision, room, bot, and fake-client testing.
- Config should cover arena dimensions, tick rate, spawn rules, respawn cooldowns, starting length, speed/boost tuning, food count, room capacity, bot count, fake-client defaults, render/debug camera knobs, tail width, map outline width, sound volumes/speed thresholds/spatial attenuation, network input delay, and debug tracing.
- Game logic should consume typed config resources rather than scattering constants through systems.
- `FoodConfig.max_count` is a hard per-room cap. Regular timed spawns target `min(target_count, max_count)`, and death food drops also respect remaining room capacity so repeated deaths cannot create unbounded food.
- Admin credentials are secrets, not game config. Local runs use ignored `secrets/admin.env` with `LIGHTRIDER_ADMIN_PASSWORD`; deployment should inject the same env var through the server's secret/env mechanism.

### Multiplayer And Rooms

- The game will be served online through Edgegap.
- Users can join a random room or create a new room.
- Browser users can also enter a four-letter private room code. The code maps to a stable private `RoomId`; joining the same code should route clients to the same private room when capacity permits.
- A single server process should support multiple game rooms through the room system.
- The authoritative server owns room simulation, player spawning, bot spawning, food spawning, collision, scoring, death, and respawn.
- Clients render the world and send input. They do not decide kills, food pickups, or score.

### Networking Model

- Use Bevy `0.18` and Lightyear from the `main` branch of `https://github.com/cBournhonesque/lightyear.git`. Cargo may still report Lightyear's crate version as `0.26.4`; the source of truth is the git dependency and commit in `Cargo.lock`.
- Simulation remains fixed at `movement.tick_rate_hz` in RON, currently `30.0` Hz. Server state replication is separately data-driven through `network.replication_send_hz`, currently `10` Hz, matching the original Powerline client cadence of interpolating over three 30 Hz ticks (`INTERP_TIME = (1000 / 30) * 3`). Lightrider wires this into Lightyear's `ReplicationMetadata` resource as a 100 ms send interval.
- Snake `TailPoints` are now replicated through Replicon's op-delta rule using the local `/spare/ssd/cbournhonesque/src/other/bevy_replicon` checkout. Movement records tail operations (`SetFrontPosition`, `SetFrontDirectionAndPush`, `ShortenBy`) so normal movement sends compact operation history instead of the full tail component every replication send.
- The deployment plan has pivoted back to WebTransport so browser WASM clients can connect directly to Edgegap-hosted game servers.
- Bevygap is the planned matchmaking/control path: clients talk to `bevygap_matchmaker_httpd` over WebSocket, receive a Lightyear `ConnectToken` plus WebTransport certificate digest, and then connect to the game server over WebTransport.
- The direct Lightrider client/server path uses WebTransport and remains the default local/dev mode.
- The first Bevygap integration slice is runtime-tested behind optional Cargo features. A `client --features bevygap -- --matchmaker-url ...` run spawns an unconnected Lightyear client entity and lets `bevygap_client_plugin` request a token and attach token auth, peer address, and WebTransport IO. A `server --features bevygap -- --bevygap` run adds the server-side Bevygap/NATS plugin; direct mode does not read Edgegap or NATS env vars.
- Bevygap local mock validation works through `just bevygap-local-smoke`: local NATS + in-process `BEVYGAP_CONTEXT_MODE=local` server context + Lightrider WebTransport server + mock `bevygap_matchmaker` + `bevygap_matchmaker_httpd` + headless Lightrider bot client. The client receives a redacted `Session Ready` response, decodes the Lightyear `ConnectToken`, connects over WebTransport, and the server and matchmaker both observe the active-connection path.
- Production netcode identity is environment-driven. `LIGHTRIDER_PROTOCOL_ID` and `LIGHTRIDER_PRIVATE_KEY` must match between game server and matchmaker; `LIGHTRIDER_REQUIRE_PRODUCTION_NETCODE=1` makes zero/dev defaults a startup error. `LIGHTRIDER_PRIVATE_KEY` accepts either 64 hex characters or the Bevygap-compatible comma-separated 32-byte form.
- Edgegap should be treated as coarse deployment/session capacity. Lightrider rooms are game-specific state and should be routed by Bevygap, not by Edgegap directly. A warm Edgegap deployment can host multiple public/private rooms until game-server capacity is reached.
- Bevygap also supports an optional static/self-hosted deployment path. Static game servers publish the same deployment metrics as Edgegap-hosted servers, tagged with provider/country/region. Public room requests only prefer static deployments when GeoIP maps the client IP to an allowed country such as `US`; private/specific room requests intentionally ignore that location filter so invited players can reach the same private room.
- Client prediction is enabled only for the local player's snake.
- Other player snakes are interpolated, not predicted.
- Movement is predicted, but deaths are server-authoritative.
- Local food boost is predicted for the controlled snake: after predicted movement, the client checks known static food positions with the same pickup radius as the server and applies only `FoodBoost`. Tail growth, score, food despawn, sounds, stats, and pickup visuals remain server-confirmed; confirmed `FoodCollision` is fanned out locally so sound and pickup animation can both react.
- Visual correction is intentionally not configured right now; frame interpolation is used.
- Bots controlled by the server should reuse shared steering/simulation where possible.
- Fake clients are the normal client binary with `--headless --mode bot`; they connect through the real protocol, send real movement input, and send normal reliable respawn messages when needed.

## General Agent Notes

### Architecture Goals

- Keep a Cargo workspace.
- Target top-level organization is four main areas:
  - `client`: client-specific app logic, input sources, connection flow, local prediction setup, client modes, and headless/fake-client behavior.
  - `server`: authoritative simulation orchestration, room lifecycle, bot orchestration, Edgegap-facing runtime config, and production logging.
  - `render`: rendering-specific logic, camera, UI, debug views, visual interpolation display, and cosmetic systems. This is still folded into the client crate today.
  - `shared`: network protocol, deterministic movement, collision geometry, room-safe components, config types, simulation rules, and reusable bot decision helpers.
- Do not use Avian or another physics engine for core gameplay. Collision, proximity, friction/boost, food overlap, and boundaries should remain explicit geometry.
- Rust modules with nesting should use `a.rs` and `a/b.rs`, not `mod.rs`, when adding or reorganizing modules.
- Add focused unit tests for complicated logic: tail shortening, tail interpolation, turn validation, collision cases, proximity boost, food spawn constraints, room assignment, and bot/fake-client input generation.

### Current Repo Snapshot

- Workspace members: `client`, `server`, `shared`, and `web_client`.
- Dependency state: Bevy `0.18.1`, Lightyear git `main` at the commit locked in `Cargo.lock`, `bevy_enhanced_input = "0.24.4"`, `bevy_turborand = "0.13"`, `bevy-inspector-egui = "0.36"`, and no direct physics-engine dependency.
- Lightrider transport features currently enable Lightyear WebTransport, self-signed WebTransport certificates, and the dangerous native no-certificate-validation test path.
- Data-driven config lives in `shared/src/config.rs` with RON files at `config/default.ron` and `config/test.ron`.
- `MovementConfig::tick_duration()` is the source for the Lightyear fixed tick duration.
- Shared movement lives in `shared/src/movement/mod.rs` and includes BEI-driven turn handling, config-driven acceleration/speed integration, food-pickup acceleration boost decay, and tail shortening.
- Shared explicit geometry lives in `shared/src/utils/geometry.rs`, `shared/src/collision/collider.rs`, and `server/src/collision/collider.rs`.
- Simulation-critical movement, proximity, server collision/death, and food overlap/growth run in `FixedUpdate` around `SimulationSet::Movement`.
- `RoomId` is registered in the protocol and attached to maps, players, snakes, and food. Proximity boost, collision, food overlap, respawn, rank computation, and Lightyear visibility are room-scoped.
- Food spawning, overlap, growth, and death drops use `GameConfig`. Food positions are no longer magnetically moved by the authoritative server.
- Broad-phase spatial indexing has started in `shared::spatial`. Tail segments are indexed in two room-scoped 1D hash grids by orientation (`vertical` keyed by `x`, `horizontal` keyed by `y`) while keeping exact ray/segment narrow-phase checks. Food positions are indexed in a deterministic room-scoped 2D KD-tree and sorted by distance plus entity id for stable pickup prediction.
- Food uses Lightyear `Position` interpolation. Eaten food should be despawned in a way that lets Lightyear record delayed interpolated despawns.
- `PlayerScore`, `PlayerRank`, and `PlayerStatus` replicate with the player.
- Death flow uses server-internal `SnakeCollision` with `DeathReason` and sends a server-to-client `PlayerDeath` message.
- Client death view follows the killer after a collision death if possible, otherwise stays static. Respawn is requested with Enter or Space after cooldown.
- Snake movement uses Lightyear's BEI input plugin with replicated action entities. Client setup keys off Lightyear's `Controlled` marker. Respawn is intentionally not an input stream: clients send a reliable `PlayerSpawnRequest` message, the server validates cooldown/room/player state, and then spawns the snake plus movement action entities.
- Client/server connection setup uses Lightyear 0.26 entity components (`ClientPlugins`, `ServerPlugins`, `NetcodeClient`, `NetcodeServer`, `WebTransportClientIo`, `WebTransportServerIo`).
- Direct native clients can pass `--cert-digest` or `--certificate-digest`; copied colon-separated server log digests are normalized before connection. Leaving it empty uses the native dangerous no-validation test path. Browser clients must receive the digest from matchmaking/bootstrap.
- Optional Cargo feature `bevygap` gates Lightrider's path dependencies on the local Lightyear matchmaker client/server/core/NATS crates. The default build must not compile or require NATS, Edgegap credentials, or matchmaker services.
- Optional client feature `bevygap-matchmaker-tls` is currently a compatibility alias for the matchmaker-enabled path; browser `wss://` support comes from the wasm WebSocket stack, while native TLS support should be rechecked before relying on native `wss://`.
- The local `lightyear_matchmaker_bevy_client` path dependency must stay wasm-compatible. Its WebSocket implementation is target-specific: native builds use `tokio-tungstenite` on a Tokio runtime thread, while wasm builds use `tokio-tungstenite-wasm` plus `wasm_bindgen_futures::spawn_local` so `mio`/native Tokio net features are not pulled into `wasm32-unknown-unknown`.
- Client networking now has two startup modes: direct WebTransport/manual auth, or Bevygap token mode. In token mode the client entity starts with Lightyear base components and waits for the Bevygap plugin to connect it.
- Browser builds have a dedicated `lightrider-web` WASM entrypoint. It avoids Clap and defaults the matchmaker WebSocket to same-origin `/matchmaker/ws`, with `?matchmaker_url=` as an override.
- Browser UI now lives in the `web_client` workspace crate. It is a Leptos CSR shell using `leptos-bevy-canvas` to mount the existing Bevy client in the background canvas while web UI owns the Powerline-style join modal, player-name input, private room code input, persistent private-room badge, closeable menu, and external links.
- Native clients set Bevy's asset root to `../assets` because Cargo runs the `client` package with `client/` as the Bevy project root, while Lightrider stores shared runtime assets at the workspace root. WASM clients keep the default `assets` root because `just web-build` and the matchmaker Dockerfile copy assets into `web/assets`.
- Browser startup/status messages are split between static HTML and Bevy state. `web/index.html` shows `Loading assets...` before WASM initializes. The Leptos shell now performs a browser capability preflight and refuses to start Bevy/matchmaking when WebTransport cannot work, for example from a non-secure public `http://` origin. After Bevy starts, `client::web_status` updates the same DOM element from `BevygapClientState`: `Waiting for new server to start` for fresh Edgegap deployments, `Waiting to connect to server` for warm deployments/connection, and hidden once Lightyear connects.
- WASM browser clients rely on Lightyear's built-in `lightyear_web::WebKeepalivePlugin` rather than adding a second Lightrider-level keepalive plugin. Lightyear currently wakes hidden tabs at a lower fixed cadence, which keeps network IO moving without duplicating worker wakeups.
- Browser query parameters currently drive initial Bevy startup: `name`, `room`, `matchmaker_url`, `matchmaker_game`, and `matchmaker_version`. `room` accepts `auto`, `new`, a four-letter private room code, or a numeric room id. The modal writes settings back to the URL; replacing this URL reload with a Leptos-to-Bevy message bridge is the next polish step.
- Bevygap room-aware matchmaking is implemented in the local Bevygap repo. Clients include `RoomSelection` in `RequestSession`; HTTPD forwards the full request; game servers publish deployment room metrics to NATS KV; the matchmaker reuses an existing Edgegap deployment by setting `SessionModel.deployment_request_id` when capacity policy allows.
- Matchmaker capacity policy is CLI/env driven: `--max-players-per-deployment`, `--max-rooms-per-deployment`, and `--max-cpu-percent-per-deployment` are wired through local just recipes and the matchmaker/control container as `BEVYGAP_MAX_PLAYERS_PER_DEPLOYMENT`, `BEVYGAP_MAX_ROOMS_PER_DEPLOYMENT`, and `BEVYGAP_MAX_CPU_PERCENT_PER_DEPLOYMENT`.
- CPU-based matchmaker placement is game-server reported. Lightrider's Bevygap heartbeat publishes room capacity and process CPU together in `DeploymentMetrics`; the Linux sampler reads `/proc/self/stat`, normalizes against cgroup CPU quota when available, and falls back to Rust's available parallelism. Edgegap exposes deployment metrics through its API, but it does not push CPU readings into our NATS KV automatically.
- Client input systems tolerate the token-mode startup window before `LocalId` is available.
- Initial player spawning is gated on client timeline sync. The client now waits for `IsSynced<InputTimeline>` before sending its `RoomJoinRequest` and player name. The server no longer auto-spawns on raw `Connected`; it creates the player/snake when the room join request arrives, and stores a pending player name on the connection so name updates that arrive before spawn are not lost.
- Non-headless clients must spawn `ReplicationReceiver::default()` on the client entity, or the network connection can succeed without replicated game entities arriving.
- Custom snake interpolation uses `ConfirmedHistory<TailPoints>`, `ConfirmedHistory<TailLength>`, and `InterpolationSystems::Interpolate`, adapted from the old prototype and the Lightyear `replication_groups` example.
- Frame interpolation is enabled for predicted snake `TailPoints` and `TailLength` through Lightyear's `FrameInterpolationPlugin` plus `FrameInterpolate` components. Remote interpolated snakes still use Lightrider's custom length-aware tail interpolation system. Predicted visual-correction functions exist for `TailPoints`/`TailLength`, but they are intentionally not registered while we validate whether correction smoothing causes artifacts.
- Server-owned bots live in `server/src/bots.rs` and reuse shared bot steering from `shared/src/bot.rs`.
- Secret admin mode is server-authoritative. The client only reveals the admin UI with a hidden shortcut; the server checks `LIGHTRIDER_ADMIN_PASSWORD`, marks the connection as admin, and currently accepts only `set_num_bots` for the admin's current room. Bot targets are per-room overrides and can spawn or remove in-game server bots without creating fake network clients.
- Runnable binaries are named `lightrider-server` and `lightrider-client`.
- The root `justfile` has `server`, `client`, `client-debug`, `bot`, `bots`, `local`, `trace-local`, `trace-summary`, and Edgegap image recipes.
- Bevygap local-smoke recipes now include `bevygap-help`, `bevygap-nats-pull`, `bevygap-nats`, `bevygap-nats-health`, `bevygap-fake-context`, `bevygap-server-local`, `bevygap-matchmaker-local`, `bevygap-matchmaker-mock-local`, `bevygap-matchmaker-httpd-local`, `bevygap-matchmaker-mock-stack-local`, `bevygap-client-bot`, and `bevygap-local-smoke`. Use `bevygap-help` for the required order.
- `bevygap-local-smoke` writes service logs under `logs/bevygap/<timestamp>/` and updates `logs/bevygap/latest`. It scans for cert digest publication, mock session creation, redacted session readiness, client connection attempt, server-side Lightyear connect event, server active-connection KV write, and matchmaker active-connection watcher observation.
- Web-server/control-host setup is scripted by `tools/setup_web_server_host.sh` and wrapped by `just deploy-web-server`, `just deploy-web-server-pull`, `just web-server-env-template`, `just web-server-install`, and `just web-server-health`. The installer runs the bundled matchmaker/control image as a systemd-managed Podman container with web on `80/tcp`, NATS on `4222/tcp`, and NATS monitoring bound to local `8222/tcp`. VPS recipes accept `ssh_key=...` when the required identity is not already in the SSH agent/default identities.
- `Dockerfile.matchmaker` defaults to a balanced 32G+ RAM release build for the control image: two Cargo jobs, thin LTO, multiple codegen units, and `lld` for the native Bevygap binaries. Smaller hosts can override to `MATCHMAKER_CARGO_JOBS=1 MATCHMAKER_RELEASE_LTO=false MATCHMAKER_RELEASE_CODEGEN_UNITS=16 WEB_CARGO_JOBS=1`. The Dockerfile also installs `wasm32-unknown-unknown` after copying the repo so Rustup applies it to the active `rust-toolchain.toml` toolchain.
- Browser UI local testing is available through `just web-build` and `just web-serve`. The local browser path expects the manual Bevygap mock stack on NATS + `bevygap-server-local` + `bevygap-matchmaker-mock-stack-local`, then opens `http://localhost:8000/?matchmaker_url=ws://127.0.0.1:3000/matchmaker/ws&matchmaker_game=lightrider&matchmaker_version=dev`. Through SSH port forwarding, forward both web and matchmaker TCP ports; the full remote browser game still needs a separately reachable WebTransport/UDP game port.
- Hybrid Edgegap testing is possible with the game server on Edgegap and the matchmaker/web server local, but only if the Edgegap container and the local matchmaker share a NATS endpoint that Edgegap can reach. A plain local `127.0.0.1:4222` NATS cannot complete this flow.
- Hybrid production routing can also run one static Lightrider game server on the control VPS. The setup script can start `lightrider-static-server` with `--network host`, local Bevygap context mode, `BEVYGAP_DEPLOYMENT_PROVIDER=static`, and UDP/WebTransport port `7777`; Edgegap is then used only when policy says the static deployment should not be used or has no capacity.
- Public static routing needs `BEVYGAP_GEOIP_DB` to point at a MaxMind GeoLite2/GeoIP2 country database readable inside the matchmaker/control container. If the DB is missing or the client country is unknown, public `auto`/`new` requests do not prefer static; private room codes and explicit room ids ignore location by design.
- Production netcode currently uses `LIGHTRIDER_PROTOCOL_ID=1`; the private key is stored locally in ignored `secrets/prod-netcode.env` and must be copied into both the Edgegap game-server app version and the matchmaker/control host env.
- Matchmaker/control image builds can be done locally and pushed to the Edgegap registry; the VPS can then use `SKIP_IMAGE_BUILD=1 just deploy-web-server ...` to pull/run only. `matchmaker-build` and `deploy-web-server` accept Podman build resource kwargs such as `memory=24g`, `cpus=8`, `cpu_quota=...`, and `cpuset_cpus=...`.
- Edgegap app-version automation defaults `EDGEGAP_FORCE_CACHE=false` because the current Edgegap organization rejects enabled image cache with quota `0`. The Edgegap API accepts `req_cpu`/`req_memory` on app-version create but rejects them on update, so sync uses a PATCH-safe update payload and then verifies the refetched deploy-critical state. The verifier normalizes Edgegap-managed/default fields and checks deploy-critical state instead of byte-for-byte API echo.
- Production Dockerfiles use `cargo-chef` to split dependency and app build layers. The context includes `lightrider`, `bevygap`, and `lightyear`; Lightrider source-only changes should mostly hit final app layers, but sibling path dependencies are copied into cook stages so source changes in `bevygap` or `lightyear` can invalidate dependency layers. Local image recipes use Podman `--layers` by default and accept optional `PODMAN_CACHE_FROM`/`PODMAN_CACHE_TO` for explicit remote cache experiments.
- GitHub Actions release image builds live in `.github/workflows/release-images.yml`. They run only on `release.published`, check out `lightrider` at the release ref, fall back `cBournhonesque/lightyear` and `cBournhonesque/bevygap` to `main` unless `LIGHTYEAR_REF`/`BEVYGAP_REF` are set, optionally use `CHECKOUT_TOKEN` for private dependency repo access, build with Docker Buildx/GHA cache scopes, and push `lightrider-server:<release-tag>` plus `lightrider-matchmaker:<release-tag>` to the Edgegap registry.

### Observability And Debugging

- Runtime debug tracing lives in `shared/src/debug.rs`.
- Resource policy for this repo: use Cargo with at most two jobs (`-j 2` or `CARGO_BUILD_JOBS=2`). Avoid starting overlapping Cargo builds unless explicitly needed. Dev builds keep incremental compilation enabled for iteration speed, but reduce disk pressure by keeping only line-table debug info for local crates and no debug info for dependencies/build scripts.
- When `GameConfig.debug.lightyear_debug` is true and `LIGHTYEAR_DEBUG_FILE` is set, client/server `LogPlugin`s install a Lightyear-compatible JSONL layer for `lightyear_debug::*` tracing targets and enable `lightyear_debug=trace`.
- Runtime tracing now emits low-rate `perf_frame` rows even when `json_snapshots` is false. These rows contain Bevy frame delta/fps and aggregate Lightyear link RTT, jitter, and send/receive buffer counts for the traced process.
- JSONL trace rows include a process-local `frame_index`, which lets load summaries group Lightyear transport fragment rows by Bevy frame.
- `json_snapshots` emits `snake_head` rows and `snake_invariant_violation` rows for DuckDB analysis.
- `config/load.ron` is the dedicated headless load-test config: one room can hold at least 100 clients, server-owned bots and sound are disabled, food density is intentionally high, and JSON snapshots are disabled while Lightyear/perf trace capture remains available for sampled processes. `config/load_no_food.ron` keeps the same load-test shape but disables spawned food and death-food so replication traffic can be compared against the food-heavy load profile.
- `just load-local` runs a full local load test with a headless server, many headless bot clients, OS-level CPU/memory/network samplers, low-rate server/client Lightyear traces, and a summary under `logs/load/<timestamp>/`. It defaults network sampling to `lo` to avoid unrelated external traffic in local measurements; override with `net_interfaces=eth0` or a comma-separated list when needed. `just fake-clients` launches only the bot clients against an existing local, Edgegap, or manually started server. `just load-summary` re-summarizes an existing run.
- `just load-local` must execute binaries from `${CARGO_TARGET_DIR:-target}`. A 2026-06-03 compression investigation found that earlier shared-target runs built into `/spare/local/cbournhonesque/cargo-target` but executed stale binaries from `target/`, which made Lightyear packet compression appear inactive.
- `just profile-chrome` runs a local headless server plus bot clients with the server built using the optional `server/profile-chrome` Cargo feature (`bevy/trace_chrome` + `bevy/detailed_trace`). The recipe sets `TRACE_CHROME`, stops the server through the graceful `--run-seconds` path so the Chrome trace flushes, captures process/network samples, and writes an AI-readable summary under `logs/profile/<timestamp>/chrome/`. It defaults `incremental=false` to avoid creating a large one-off incremental cache for the profiled Bevy feature set; pass `incremental=true` if rebuild speed matters more than disk.
- `tools/chrome_trace_summary.py TRACE_JSON [OUT_DIR]` parses Bevy/tracing Chrome trace JSON and emits `system_profile.csv`, `thread_profile.csv`, and `profile_summary.md`. It reports top spans by total time and p95 latency, merged busy time by process/thread, and estimated average/peak parallelism. Inclusive span totals can double-count nested spans; use merged thread busy time when reasoning about actual wall-clock parallelism.
- `just trace-local` builds binaries once, runs a headless WebTransport server plus multiple staggered headless bot clients, writes `.ndjson` and `.log` files under `logs/debug/<timestamp>/`, and runs `tools/debug_trace_summary.sql`.
- `just trace-local-mixed` builds binaries once, runs one headless server, one headless player client, configurable headless bot clients, and whatever server-owned bots the config enables. It writes `.ndjson` and `.log` files under `logs/debug/<timestamp>-mixed-headless/`, updates `logs/debug/latest-mixed`, and runs the same DuckDB summary. Use this for mixed prediction/interpolation debugging without opening a rendered client.
- `config/debug_no_food.ron` and `config/debug_solo.ron` are dedicated trace configs for isolating baseline prediction. Both enable JSONL tracing and disable server bots; `debug_no_food` also disables food entirely.
- `just trace-summary dir=...` reruns the DuckDB summary on an existing trace directory.
- `tools/debug_trace_summary.sql` now summarizes perf frame/link metrics, transport packet rows, packet fragments per frame, rollback requests by process, rollback delta histogram, rollback mismatch component counts, and stale confirmed-history mismatch component counts before the movement tables.
- Server snake collisions emit `lightyear_debug::manual` rows with kind `server_snake_collision`, including the killed head sweep, hit distance, and exact tail segment hit. Use this to diagnose reports of "invisible" deaths against the client-visible interpolated state.
- `just bevygap-local-smoke` is the native token-path smoke. It uses local NATS and the server plugin's local context mode, then verifies the matchmaker WebSocket -> NATS -> mock Edgegap session -> Lightyear `ConnectToken` -> WebTransport connect -> active-session KV flow. The smoke intentionally checks redacted log text and should not emit raw connect tokens.
- The client Bevy HUD exposes a small in-game network panel for player-visible ping/RTT and jitter, separate from Leptos/browser chrome.
- The secret admin panel exposes Lightyear `PredictionMetrics` rollback counts, total rollback ticks, and average rollback depth while also providing room bot target controls. This is useful for quick visual correlation, but local JSONL traces remain the durable debugging source.
- Local admin recipes must export secrets after sourcing `secrets/admin.env`. Plain `KEY=value` secret files are not inherited by child processes unless sourced under `set -a` or written with `export KEY=value`; the local `just` server recipes now do this automatically.
- The rendered client disables Bevy asset metadata checks and relies on default loader settings. This avoids browser/static servers returning HTML or 404 bodies for missing `*.meta` sidecars and causing `Failed to deserialize meta` errors for Powerline PNG/OGG assets.
- AI agents should be able to autonomously run a headless server plus multiple headless bot clients, capture structured logs, load them into DuckDB, and identify simulation/replication bugs without manual visual inspection.
- 2026-05-31 headless diagnosis: a no-food/no-bot trace had one startup `TailPoints` rollback and then stable motion; the same single-player trace with food produced small `TailLength` corrections from server-authoritative food growth; a mixed player + two fake-client bots + four server bots trace produced only 1-3 tick rollbacks, dominated by `TailLength` and `FoodBoost`. Baseline movement/timeline sync looks stable; current stutter suspects are food-authoritative correction bursts, remote interpolation/visual delay around collisions, and render/camera smoothing rather than large rollback spikes.
- 2026-06-01 load-test smoke: `just load-local clients=50 seconds=20 release=false port=5166 trace_clients=2 ramp_per_second=25 net_interfaces=lo` completed, but the dev server was saturated. Summary in `logs/load/20260601-105633-local-c50`: server avg CPU `147.7%`, peak CPU `197.8%`, avg frame `168.71ms`, peak frame `481.59ms`, avg fps `16.9`, avg traced RTT about `240ms`; 50 clients peaked at `944.9%` aggregate CPU and `4.75GiB` RSS; loopback averaged `42Mbps` and peaked near `100Mbps`; traced clients saw about `858` received fragments each with max `16` fragments/frame; traced prediction rollbacks were `17` with avg delta `12.12` and max `21`. This is a useful stress result but not a production-capacity number because it was a dev build and full transport tracing made `1.9GiB` of logs.
- When debugging deployed WebTransport, compare client timeout logs with server logs. The server should log `Starting WebTransport server`, the certificate digest, and Lightyear WebTransport startup before clients attempt to connect.

## Implementation Plan

### Phase 0: Baseline And Dependency Upgrade

Status: complete.

1. Upgraded the workspace to Bevy `0.18.1` and Lightyear main.
2. Introduced typed data-driven RON config loading with default and test configs.
3. Removed direct physics-engine usage and replaced gameplay checks with explicit geometry.
4. Restored workspace checks/tests.

### Phase 1: Deterministic Shared Simulation

Status: complete enough to build on.

1. Movement, speed, boost, spawn length, arena, food, room, and Lightyear fixed tick settings are config-driven.
2. Boundary collision, room-scoped snake collision, proximity boost, and food overlap are explicit geometry.
3. Simulation-critical systems run on fixed tick schedules.
4. Focused tests exist for boundary, boost helpers, room-scoped proximity, room-scoped snake collision, and room-scoped food overlap.

### Phase 2: Lightyear Protocol And Prediction

Status: complete enough for prototype validation.

1. Protocol registration is rebuilt against the upgraded Lightyear API.
2. Runtime replication covers player identity, snake state, tail points, food, room id, score, status, and death messages. Leaderboard rank/stat presentation is sent separately as a low-rate unreliable snapshot.
3. WebTransport is now the active direct client/server transport; the online/browser path will add Bevygap token matchmaking on top.
4. Only the local player's snake is predicted. Remote snakes are interpolated.
5. Deaths, kill reasons, food pickups, scoring, and respawn state stay server-authoritative.
6. Remote snake interpolation and predicted-tail frame interpolation are in place.

### Phase 3: Authoritative Server Rooms

Status: complete enough for deployable prototype work.

1. Room entities/resources exist in `server/src/rooms.rs`.
2. Clients can auto-join a public room, create a new public room, request a numeric room id, or request a four-letter private room code through `RoomJoinRequest`.
3. Room config covers max rooms, max players per room, arena size, food target, and bot target counts.
4. Replication, collision, food, proximity, respawn, and ranking are room-scoped.
5. Public auto-join selection ignores private-code rooms so random players do not backfill private games.
6. Minimal leaderboard/rank state exists on the server as `PlayerRank`; clients consume room-scoped `LeaderboardSnapshot` messages instead of direct rank/stat component replication.

### Phase 4: Bots And Fake Clients

Status: complete enough for local load/latency smoke work.

1. Server-owned bots choose legal turns with shared deterministic steering and use normal simulation systems.
2. Fake clients run as `lightrider-client --headless --mode bot` and send real network inputs.
3. Local test modes are available through `just bot`, `just bots`, and `just local`.
4. Initial operational signals are connection logs, room assignment logs, replicated score/rank/status, and smoke-test log scanning.

### Phase 5: Deployable Prototype

Status: in progress.

1. The game-server image is `Dockerfile.server`. It builds `lightrider-server --features bevygap`, defaults to `--bevygap`, and exposes `7777/udp` for WebTransport/QUIC.
2. The matchmaker/control image is `Dockerfile.matchmaker`. It bundles NATS, `bevygap_matchmaker`, `bevygap_matchmaker_httpd`, nginx, and the compiled `lightrider-web` WASM files.
3. Edgegap registry credentials and API settings are stored locally in ignored `secrets/edgegap.env`.
4. The server image tag shape is `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-server:<tag>`.
5. The matchmaker image tag shape is `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-matchmaker:<tag>`.
6. Current deployment direction: run the matchmaker/control image on a VPS or equivalent public host, and run the game-server image on Edgegap.

### Edgegap And Web Client Hosting Plan

Goal: publish a WebTransport Edgegap server image and a browser WASM client. The page starts the Bevy client, talks to Bevygap matchmaking over WebSocket, receives a Lightyear `ConnectToken` and certificate digest, then connects to the Edgegap game server over WebTransport.

Reference docs for this plan:

- Edgegap deployments: `https://docs.edgegap.com/docs/deployment/automated-deployment`
- Edgegap application versions: `https://docs.edgegap.com/learn/advanced-features/application-and-versions`
- Edgegap container registry: `https://docs.edgegap.com/docs/edgegap-container-registry`
- MDN WebTransport certificate hashes: `https://developer.mozilla.org/docs/Web/API/WebTransport/WebTransport`

1. Server container:
   - Build `lightrider-server --release --features bevygap` in a multi-stage container.
   - Runtime image should contain only the server binary, config, and entrypoint.
   - Entrypoint should run `lightrider-server --headless --bevygap --port ${PORT:-7777} --config ${LIGHTRIDER_CONFIG:-/app/config/default.ron}` by default.
   - Publish one Edgegap `gameport` mapping for WebTransport. The internal port can remain `7777`, but the Edgegap app version protocol must match the WebTransport-compatible deployment setup rather than raw UDP.
   - Required production env includes `LIGHTRIDER_PROTOCOL_ID`, `LIGHTRIDER_PRIVATE_KEY`, `LIGHTRIDER_REQUIRE_PRODUCTION_NETCODE=1`, `NATS_HOST`, `NATS_USER`, `NATS_PASSWORD`, Edgegap/Arbitrium context env, and optional NATS TLS trust env.
   - Use Edgegap injected variables such as `ARBITRIUM_REQUEST_ID`, `ARBITRIUM_PUBLIC_IP`, `ARBITRIUM_PORTS_MAPPING`, and port-specific variables for diagnostics.

2. Registry and image publishing:
   - Keep Edgegap and registry credentials in ignored local/CI secrets.
   - Local image publishing uses `just edgegap-build`, `just edgegap-push`, and `just edgegap-build-push`.
   - Matchmaker/control publishing uses `just matchmaker-build`, `just matchmaker-push`, and `just matchmaker-build-push`.
   - `just prod-images-build` builds both production images locally; `just prod-images-push` pushes both.
   - The build context is generated under ignored `.edgegap-build/` because Lightrider currently depends on sibling `../lightyear` and optional sibling `../bevygap`.
   - Prefer immutable tags for future CI. The mutable `dev` tag is convenient but can confuse deployment diagnosis.
   - Before testing a deployment, verify the app version's `docker_image` and `docker_tag` match the intended Lightrider image.

3. Web client:
   - Current browser entrypoint is `lightrider-web`.
   - Browser entrypoint should read player name, room join mode, server host/port, and bootstrap data from URL params or fetched JSON.
   - The matchmaker/control image currently hosts static client files through nginx on `WEB_PORT` (default `8080`) and proxies `/matchmaker/*` to `bevygap_matchmaker_httpd`.
   - Raw UDP will not work in browser WASM; WebTransport is the chosen browser transport for this plan.

4. Matchmaking/control service:
   - Do not call Edgegap APIs directly from the browser. The Edgegap token must stay server-side.
   - Use `/spare/ssd/cbournhonesque/src/other/bevygap` as the starting point.
   - All Bevygap/Edgegap integration in Lightrider must be behind an optional Cargo feature, initially named `bevygap`. The default build must continue to support direct local WebTransport without pulling in Bevygap, NATS, Edgegap API, or extra service dependencies.
   - Relevant Bevygap utilities:
     - `bevygap_matchmaker_httpd`: public HTTP/WebSocket frontend; clients connect to `/matchmaker/ws`.
     - `bevygap_matchmaker`: private NATS service that creates Edgegap sessions, waits for readiness, generates Lightyear `ConnectToken`s, and schedules deletion.
     - `bevygap_shared`: shared `RequestSession`, `SessionRequestFeedback`, NATS bucket/stream setup, and protocol structs.
     - `bevy_nfws`: Bevy WebSocket client wrapper.
     - `bevygap_client_plugin`: reference client state machine for matchmaking and token-based Lightyear connect.
     - `bevygap_server_plugin`: reference server-side Edgegap context/NATS integration and active connection tracking.
     - `bevygap_webhook_sink`: optional Edgegap webhook receiver.
     - `edgegap_async`: generated Edgegap API client used by the matchmaker.
   - Preferred flow:
     1. Game client connects to `bevygap_matchmaker_httpd` over WebSocket.
     2. Client sends `RequestSession { game, version, client_ip, room }`, where `room` is auto, new, a private code, or a numeric room id.
     3. HTTPD publishes a NATS request to `bevygap_matchmaker`.
     4. Matchmaker reads game-server deployment metrics from NATS KV. For public auto-join it chooses a not-full public room or a warm deployment with room capacity; for private codes and numeric room ids it prefers the deployment already hosting that room; otherwise it creates a room on a warm deployment if capacity allows. If no warm deployment passes policy, it creates a new Edgegap session/deployment.
     5. When a new deployment is needed, the matchmaker creates an Edgegap session, waits until ready, reads public IP/external port, assigns a Lightyear client id, builds a `ConnectToken`, stores session/client mappings in NATS KV, and streams feedback.
     6. Client receives `SessionReady`, decodes token, configures the Lightyear connection, and connects to the game server.
     7. Game server handles the room request through normal `RoomJoinRequest` logic and reports active connection state through NATS.
     8. Matchmaker deletes sessions after disconnect or after unclaimed-session timeout.
   - NATS role in current Bevygap:
     - NATS core request/reply links `bevygap_matchmaker_httpd` to `bevygap_matchmaker`.
     - NATS JetStream KV stores client-id to Edgegap-session mappings, active connections, and WebTransport certificate digests.
     - Edgegap-hosted game servers must reach the same public NATS service so they can publish their context/digest and active connection state.
     - Game servers publish a small room-capacity heartbeat to the `deployment_metrics` KV bucket: deployment/request id, endpoint, current rooms, public/private marker, current humans, max rooms, max players per room, and optional CPU percentage.
     - Edgegap deployment CPU/memory/network metrics are available through the Edgegap metrics API by request id, but they are not currently bridged into Bevygap's NATS KV. The matchmaker's CPU threshold uses the `DeploymentMetrics.cpu_percent` value published by the Lightrider game server.
     - In the current packaging plan, this NATS server is bundled into the matchmaker/control image for the simplest first deployment; it can be split into managed NATS or a separate VPS service later.
   - Required env vars are `NATS_HOST`, `NATS_USER`, `NATS_PASSWORD`, and optionally `NATS_CA` or `NATS_CA_CONTENTS` for TLS trust.
   - Recommended env var `BEVYGAP_NATS_NAMESPACE` must match between matchmaker/control services and Edgegap-hosted game servers for a given app/version.
   - Bevygap priority work:
     1. P0 complete: server readiness publication. The server plugin must publish context and WebTransport certificate digest only after NATS is connected, Edgegap context is loaded, the certificate digest is extracted, and the NATS sender exists. This avoids the previous one-shot `ContextLoaded` race.
     2. P0 complete baseline: production NATS setup hardening. NATS insecure mode is now an explicit truthy flag, and production mode can reject insecure NATS and default development credentials. Real deployment still needs actual TLS certificates, DNS/SAN choices, and firewall rules.
     3. P1 baseline complete: automate Edgegap app/version creation and env updates so the server image, app version, port protocol, NATS env vars, and netcode identity are less manual.
        - Current shape: `tools/edgegap_app_version.sh` plus `just` wrappers. The operator supplies image tag, Edgegap app/version, public NATS endpoint, and secret env overlay; the script creates/updates the app version and verifies that Edgegap now points at the exact server image/tag, UDP/7777 WebTransport game port, session config, and required env names.
        - Inputs should be split between checked-in defaults and ignored secrets. Checked-in config can define app name, internal port, protocol, config path, and safe env defaults. Ignored env files provide `EDGEGAP_API_KEY`, registry project, `NATS_HOST`, `NATS_USER`, `NATS_PASSWORD`, `LIGHTRIDER_PROTOCOL_ID`, `LIGHTRIDER_PRIVATE_KEY`, and optional NATS CA material.
        - Tool modes include `desired`, `show`, `diff`, `sync`, and `verify`. `diff` redacts secret values but shows missing/changed env keys, image repository/tag, port protocol, session config, and runtime env assumptions.
        - Output writes a redacted manifest under `.edgegap-build/edgegap-app-version.json` with app, version, image, tag, game port, protocol, env key list, and desired-payload hash so agents can compare deployments without printing secrets.
        - Current recipes: `edgegap-app-show`, `edgegap-app-desired`, `edgegap-app-diff`, `edgegap-app-sync`, and `edgegap-app-verify`. `edgegap-real-smoke` remains pending until public NATS is available.
     4. P1 complete: key certificate digests by deployment/request id first, endpoint `ip:port` second, and legacy public-IP keys last. This handles same-IP different-port deployments while preserving compatibility with older local data.
     5. P1 complete: add a cleaner local/mock mode for `bevygap_server_plugin`. `BEVYGAP_CONTEXT_MODE=local` synthesizes Arbitrium context from env/defaults instead of calling `ARBITRIUM_CONTEXT_URL`.
     6. P1 complete: NATS namespacing and session TTL polish. `BEVYGAP_NATS_NAMESPACE` now scopes Bevygap request subjects, gameserver announcements, delete-session streams, and JetStream KV buckets. Local recipes default to `lightrider_dev`; Edgegap app-version automation defaults to `<app>_<version>`. TTL envs exist for session mappings, unclaimed sessions, active connections, and certificate digests.
     7. P1 complete: recoverable async/runtime errors in the main Bevygap path now log or stream errors instead of panicking. The server plugin retries NATS connect/watch setup, NATS event sends handle closed channels, malformed KV values are rejected cleanly, and matchmaker/HTTPD request handling avoids request-path `unwrap`/`expect` panics.
     8. P1 complete: generated Edgegap client hygiene improved. `utils/gen-edgegap-client.sh` uses a pinned OpenAPI generator image by default, supports Podman or Docker, stores the downloaded spec under `target/`, applies only a small Cargo metadata post-process, and documents that generated source files should not be hand-edited.
     9. P1 complete: room-aware deployment packing. Lightrider servers publish per-room deployment metrics; the matchmaker uses configurable max players, max rooms, and max CPU policy before deciding whether to set `deployment_request_id` on a new Edgegap session or let Edgegap create a fresh deployment.
     10. P1 pending: run the real Edgegap-session smoke once the public NATS/control host is reachable and the app version has production env vars.
     11. P2 pending: extend `bevygap-local-smoke` with DuckDB movement assertions and add production browser bootstrap/player-name UI polish.
   - Implementation slices:
     1. Complete: add optional path dependencies under feature `bevygap`: client gates `bevygap_client_plugin`; server gates `bevygap_server_plugin`; shared code remains transport-only.
     2. Complete: add client CLI mode `--matchmaker-url <ws://...>` only when `bevygap` is enabled. Direct `--server-addr/--server-port` remains the default and stays available for local tests.
     3. Complete: refactor client network startup so it can spawn an unconnected Lightyear client entity, then either connect directly with manual auth or let Bevygap insert `NetcodeClient(Authentication::Token)`, `PeerAddr`, and `WebTransportClientIo`.
     4. Complete: add server CLI mode `--bevygap` only behind the feature. In direct mode, do not read Edgegap/NATS env vars. In Bevygap mode, add `BevygapServerPlugin` alongside the server WebTransport setup so the cert digest can be published.
     5. Complete: move `PROTOCOL_ID` and private key out of source constants for production. Direct dev can keep zero defaults, but Bevygap matchmaker and game server read the same nonzero values from env.
     6. Complete: extend `justfile` with feature-gated recipes. Local direct WebTransport remains unchanged; Bevygap recipes run NATS, `bevygap_matchmaker_httpd`, `bevygap_matchmaker`, and Lightrider with `--features bevygap`; the old fake context helper is retained but no longer needed by the default smoke.
     7. Complete: update Docker packaging/context for Bevygap. `edgegap-context` includes sibling `../bevygap`; `Dockerfile.server` builds the Bevygap-enabled game server; `Dockerfile.matchmaker` builds the matchmaker/control image with NATS and WASM assets.
     8. Partial: add a headless integration smoke that requests a token from matchmaker HTTPD and starts a bot client through the token path. `just bevygap-local-smoke` verifies the connect-token path through logs; DuckDB movement-row checks remain to be added.
     9. Complete: extend Bevygap protocol and placement for multi-room deployments. Room intent flows from browser/native client to matchmaker, room metrics flow from game server to NATS KV, and the matchmaker can reuse a warm Edgegap deployment for another room.
     10. Decide whether to keep NATS long term. If replacing it, preserve the same responsibilities: request/reply between HTTPD and matchmaker, session/client mappings with TTL/watch semantics, cert digest storage, deployment metrics, active connection tracking, and cleanup triggers.
   - Missing work:
     - Run a token-path smoke against real Edgegap session creation instead of the local mock session mode.
     - Build and run the new production images in containers; source checks, Docker dry-runs, context generation, and local native smoke pass, but full image builds were not run yet to avoid a large disk/CPU spike.
     - Extend `bevygap-local-smoke` to emit Lightyear debug JSONL and verify connection plus movement rows in DuckDB.
     - Browser UI now has a first-pass player-name/private-room modal. It still needs true spectator preview and no-reload Leptos-to-Bevy updates.
     - Stand up a public production NATS/control host with TLS, strong credentials, persistent storage, and restricted monitoring access.
   - Remaining server-side validation gap:
     - Real Edgegap active-connection validation still requires a public NATS/control host. The local mock flow now validates the server active-connection KV write and the matchmaker watcher, but it cannot prove Edgegap's real session cleanup until Edgegap-hosted game servers can reach NATS.
   - Historical runtime boundary fixes:
     - Bevygap native runtime boundaries needed fixes for this repo: the vendored Tokio task helper must not call `Runtime::block_on` when Bevy is already running inside a Tokio runtime, and native `bevy_nfws` WebSocket connections need their own Tokio runtime thread instead of Bevy's `IoTaskPool`.

## Known Bugs

- High priority: when a browser tab is hidden for too long, the client can still hit large rollback or stale interpolation/despawn-history errors. Lightyear's web keepalive should reduce throttling, but browsers can still clamp timers/network/render work; the long-term fix should be an explicit visibility/suspend/resync policy plus JSONL instrumentation, not unbounded rollback.
- Medium priority: a native `just local` rendered smoke on 2026-05-30 still emitted `Trying to do a rollback of 133 ticks. The max is 100!` shortly after startup. This did not block the admin fix, but needs instrumentation to separate startup catch-up, scheduling stalls, prediction correction, and rendering/performance causes.
- Medium priority: prediction around speed/proximity boost and large-radius food pickups needs longer mixed/browser validation. Local food boost predicts only `FoodBoost` against static known food, and the client predicts proximity boost for the controlled snake against interpolated remote tails. A short mixed trace with `food.radius=45` showed only 1-2 tick rollbacks, but food pickup contention can still produce `Speed`/`Acceleration`/`FoodBoost` corrections.
- Food boost prediction design note: do not model predicted food ingestion as player score/length/despawn truth. Current code keeps a rollback-managed `RecentlyBoostedFromFood` duplicate guard on the predicted snake, applies only the speculative `FoodBoost`, and leaves food visuals/despawns plus score/length/stats server-authoritative.
- Medium priority: mixed headless traces on 2026-05-31 show the predicted local snake can sit outside arena bounds for a couple of ticks before server-authoritative death correction arrives. That may be acceptable under server-authoritative deaths, but the invariant should either distinguish predicted pre-confirmation death windows or drive a cleaner client-side dead/pending-death state.
- Medium priority: "no visible reason" death reports need correlation with `server_snake_collision` rows. The server collision path remains centerline-only, but the client views remote snakes through interpolation while the server kills against current authoritative geometry, so a confirmed death can be against a tail segment that was not yet visually aligned on the client frame.
- Low priority: client logs can emit `client::render::snake: DIAGONAL`. This should be investigated later; current gameplay is not blocked.
- High priority QOL: camera can be slightly jittery, especially when turning or immediately after a respawn. Camera zoom growth is now smoothed, but position follow is still exact; confirm whether respawn jitter is prediction correction, camera follow, or render interpolation.
- Medium priority: validate server collision sweeps under real latency and high speed. The server now sweeps the head over the last tick's movement distance instead of raycasting only one pixel ahead, which should reduce line tunneling, but browser/client reports should still be checked against debug traces.
- Low priority: 50-client no-food load logs on 2026-06-01 emitted a few `bevy_replicon::shared::replication::signature` duplicate-hash errors where the hash already corresponded to the same entity. It did not break the benchmark, but should be understood before relying on replication signatures for debugging.
- Historical deployment blocker: Edgegap deployment `60c89b835fed` started the correct raw UDP prototype server, but external native clients timed out and the server did not log incoming datagrams. The active plan has moved to WebTransport plus Bevygap.

## Update / Changelog

- Started this document with game design, general agent notes, and changelog sections.
- Upgraded the project to Bevy `0.18.1` and Lightyear main/current git dependency.
- Added typed RON config and default/test config files.
- Replaced physics-engine collision with explicit geometry.
- Added room-scoped movement, collision, food, scoring, respawn, rank, and visibility logic.
- Added BEI input integration through Lightyear and use of `Controlled` for locally controlled entities.
- Added custom snake interpolation and frame interpolation for predicted tail state.
- Added server bots and headless fake clients.
- Added rendering baseline: dark clear color, arena outline/axes, food dots, configurable snake head/tail gizmos, death view, minimap, leaderboard, names, and debug camera shortcuts.
- Added runtime debug tracing and `just trace-local`/`trace-summary` tooling for DuckDB analysis.
- Switched the active native deployment prototype from WebTransport to UDP.
- Added `Dockerfile.server`, container entrypoint, and podman-based `just edgegap-*` recipes.
- Stored Edgegap/registry/S3 settings in ignored `secrets/edgegap.env`; the S3 secret access key still needs to be supplied before log uploads can authenticate.
- Built and pushed `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-server:dev`.
- Verified the UDP/Edgegap image work with `cargo fmt --all`, `CARGO_INCREMENTAL=0 cargo check --workspace -j 4`, `CARGO_INCREMENTAL=0 cargo test --workspace --lib -j 4`, `CARGO_INCREMENTAL=0 just trace-local 1 3 config/test.ron 5067`, container `--help`, local container startup, `just edgegap-build dev`, and `just edgegap-push dev`.
- Investigated deployment `390e2136cbf7`: Edgegap status had UDP mapping, but the app version pointed at old image `lightyear-6qgcf4w4mrq7/simple_box:a82d0d64b384e558639963c7ab5bc32c4669611b`; logs showed WebTransport certificate generation, explaining UDP client failure.
- Investigated deployment `60c89b835fed`: Edgegap status showed `139.177.195.29:30090 -> 7777/udp`, app version correctly pointed at `lightyear-6qgcf4w4mrq7/lightrider-server:dev`, and container logs showed `Starting UDP server on 0.0.0.0:7777` plus `Server UDP socket bound to 0.0.0.0:7777`. Repeated native headless clients timed out with `ConnectionRequestTimedOut`. A plain Python UDP datagram sent to `139.177.195.29:30090` also produced no server-side `Received UDP packet from new address ...` log. Current diagnosis: image and bind are correct, but UDP datagrams are not reaching the server process through the deployment path, or the Edgegap log endpoint is not returning live post-start logs. Next step is Edgegap/network-path diagnostics or a temporary UDP echo/probe build.
- Started the WebTransport + Bevygap path. Updated `/spare/ssd/cbournhonesque/src/other/bevygap` to Bevy `0.18` and local Lightyear `../lightyear/lightyear` with `client`, `server`, `netcode`, `replication`, and WebTransport features. Removed the stale `bevy_async_task` dependency from `bevy_nfws`, moved websocket tasks onto Bevy's `IoTaskPool`, updated observer APIs, updated matchmaker `ConnectToken` imports, changed the client plugin to insert `NetcodeClient`/`PeerAddr`/`WebTransportClientIo` on the existing Lightyear client entity, and changed the server plugin to observe current `Connected`/`Disconnected` ECS markers and extract certificate digest from `WebTransportServerIo`.
- Bevygap verification: `cargo check --workspace -j 4`, `cargo test --workspace --lib -j 4`, and `cargo tree -i bevy@0.18.1` pass in the Bevygap repo. `cargo tree -i bevy@0.15.1` reports no matching package, confirming the duplicate old Bevy dependency is gone. Remaining Bevygap warnings are unused/dead-code warnings in existing helper code plus one unused `Serialize` import in the HTTPD binary.
- Replaced Lightrider's direct Lightyear raw UDP IO with WebTransport. The server now generates a self-signed WebTransport identity at startup, logs its certificate digest, and includes local/Edgegap/`SELF_SIGNED_SANS` subject names. The direct client inserts `WebTransportClientIo`, accepts `--cert-digest`/`--certificate-digest`, and normalizes copied colon-separated digest logs; native local tests can leave the digest empty because the dangerous native test feature is enabled.
- WebTransport verification: `cargo check --workspace -j 4`, `cargo test --workspace --lib -j 4`, and `just trace-local 1 5 config/test.ron 5067` pass. The trace run wrote `logs/debug/20260527-132304`, logged WebTransport server startup, connected the native client with no certificate validation, produced movement rows, and reported zero stuck-snake rows and zero invariant violations.
- Added the Bevygap integration plan: Lightrider integration must be optional behind a `bevygap` feature, preserve direct local WebTransport as the default, gate client/server Bevygap dependencies separately, and use feature-gated recipes/tests for matchmaker-token smoke coverage.
- Implemented the first Lightrider Bevygap integration slice. `client` now has optional `bevygap` and `bevygap-matchmaker-tls` features, a feature-gated `--matchmaker-url` path, and split direct-vs-token connection startup. `server` now has an optional `bevygap` feature and feature-gated `--bevygap` mode. Default direct WebTransport builds do not require Bevygap/NATS/Edgegap services.
- Bevygap integration verification: `cargo fmt --all`, `cargo check --workspace -j 4`, `cargo check -p client --features bevygap -j 4`, `cargo check -p server --features bevygap -j 4`, `cargo test --workspace --lib -j 4`, default client dependency-tree check for Bevygap/NATS/Edgegap, and `just trace-local 1 4 config/test.ron 5068` pass. The feature builds are compile-checked only; the real matchmaker/NATS token flow is not yet runtime-tested.
- Reduced local build pressure: repo Cargo defaults now cap builds at two jobs, `justfile` recipes use `-j 2` plus `CARGO_INCREMENTAL=1`, dev profile keeps incremental compilation while lowering debug-info volume, dependencies/build scripts compile without dev debuginfo, Docker release builds use `-j 2` and remove build caches from the image build layer, and cleanup recipes exist for `cargo clean`, targeted incremental-cache cleanup, and `podman builder prune -f`.
- Pulled the local NATS container image with `podman pull nats:latest` and added Bevygap local-smoke recipes. Local NATS runs with JetStream, monitoring on `8222`, and development credentials `lightrider/lightrider`; the Lightrider server smoke uses a fake local Edgegap context endpoint before testing NATS context/cert-digest publication.
- Added mock Edgegap session mode to `/spare/ssd/cbournhonesque/src/other/bevygap/bevygap_matchmaker` and verified the native local Bevygap flow with `just bevygap-local-smoke 8 config/test.ron 7777 3000 9876 3001`. The passing run at `logs/bevygap/20260528-004801` covered NATS, fake Edgegap context, server cert-digest publication, mock `Session Ready`, Lightyear `ConnectToken` decode, WebTransport client connect, and server-side Lightyear connect observation.
- Hardened Bevygap local testing: native `bevy_nfws` now uses a dedicated Tokio runtime thread, the Bevygap server task helper skips nested `block_on` when Bevy already runs inside Tokio, raw connect tokens are redacted from matchmaker/HTTPD/client logs, and Lightrider disconnect cleanup no longer queues room-removal commands before immediate player/snake despawn.
- Added production netcode identity configuration. Lightrider server/client and Bevygap matchmaker now read `LIGHTRIDER_PROTOCOL_ID` plus `LIGHTRIDER_PRIVATE_KEY`, accept hex or comma-separated keys, and reject zero/dev identity when `LIGHTRIDER_REQUIRE_PRODUCTION_NETCODE=1`. `just netcode-secret` prints a fresh env block.
- Wired Bevygap admissions into Lightyear. The local Lightyear fork exposes a netcode connection-request handler setter; `bevygap_server_plugin` watches Bevygap NATS KV for issued client ids, installs the handler before starting the server, and rejects tokens whose client id was not issued by the matchmaker.
- Added production packaging for the two-image plan. `Dockerfile.server` builds the Bevygap-enabled game server image, while `Dockerfile.matchmaker` bundles NATS, `bevygap_matchmaker`, `bevygap_matchmaker_httpd`, nginx, and the `lightrider-web` WASM client files.
- Added browser entrypoint and static client shell. `lightrider-web` defaults to same-origin `/matchmaker/ws` and accepts `?matchmaker_url=`; `web/index.html` loads the generated wasm-bindgen package.
- Added image recipes for the new plan: `matchmaker-build`, `matchmaker-push`, `matchmaker-build-push`, `prod-images-build`, and `prod-images-push`. `edgegap-context` now stages sibling `../bevygap` along with Lightrider and local Lightyear for container builds.
- Verification for this slice: `cargo fmt --all` in Lightrider/Bevygap/Lightyear; `cargo check -p server --features bevygap -j 2`; `cargo check -p client --features bevygap -j 2`; wasm `cargo check` for `lightrider-web`; Bevygap plugin/matchmaker/httpd checks; `cargo test -p shared --lib -j 2`; `just --dry-run` for the production image recipes; `just edgegap-context`; `bash -n` on both entrypoints; `git diff --check`; and `just bevygap-local-smoke 8 config/test.ron 7777 3000 9876 3001`, with the latest passing logs under `logs/bevygap/20260528-102335`.
- Added `DEPLOYMENT.md` with full operator steps for direct local WebTransport testing, local Bevygap mock smoke testing, real Edgegap-session local testing, and the current two-image production deployment plan.
- Clarified the real Edgegap-session local test: it uses real Edgegap sessions/deployments through our `bevygap_matchmaker`, does not require Edgegap's managed matchmaker, and needs a NATS endpoint reachable by both the local matchmaker and the Edgegap-hosted game server. The local recipes now also accept `EDGEGAP_API_TOKEN` from `secrets/edgegap.env`.
- Real Edgegap read-only preflight on 2026-05-28 verified `lightrider/v0.0.1` is active and the matchmaker can start/listen against the real Edgegap API without creating a session. The full real-session test is blocked until the Edgegap game-server app version has public NATS env vars and matching production netcode identity; the current app version has no env vars configured.
- Fixed Bevygap matchmaker's Edgegap API base path to avoid generated `//v1/...` URLs.
- Stored Linode control-host credentials in ignored `secrets/linode.env`. Linode API access works for instance `98268043` at `45.79.138.102`, the instance is running, and the local `lightrider_linode_ed25519.pub` fingerprint matches the Linode account key. Shell SSH could not be verified from this environment because outbound TCP/22 is blocked or filtered here; `github.com:22` also times out.
- Linode firewall check: firewall `default`/`26277595` is attached to the public Linode interface, but inbound policy is `ACCEPT`, so it is permissive and not a production-ready restriction. Recommended final posture is inbound default `DROP`, allow 80/443, restricted admin SSH, NATS 4222 only as narrowly as Edgegap allows, and no public 8222/8080.
- LISH/SSH analysis on 2026-05-28: direct node SSH on 22 times out from this host; node 443 and LISH 443 both establish TCP but close before SSH banner/auth (`kex_exchange_identification`), while LISH 2200 times out. Linode profile has `lish_auth_method=keys_only` with the expected key, so the 443 LISH failure is not a rejected key. Need web LISH/Cloud Manager console or a network path that can use TCP/22 to repair/inspect sshd.
- Weblish/Glish analysis on 2026-05-28: Cloud Manager HTTPS loads, but `us-east.webconsole.linode.com:8181` and `:8080` time out from this host. The browser console failure is consistent with local/corporate network blocking the Weblish/Glish gateway ports.
- Added prioritized Bevygap work tracking to this document. P0 items are server readiness publication and production NATS setup hardening; app/version automation, cert-digest keying, local/mock server-plugin mode, real Edgegap smoke, and richer smoke assertions remain queued.
- Hardened `bevygap_server_plugin` readiness publication so context and WebTransport certificate digest publication retries until NATS, Edgegap context, cert digest, and the NATS sender all exist, then publishes once.
- Hardened Bevygap NATS configuration: `NATS_INSECURE` is now parsed as a truthy flag instead of presence-only, `BEVYGAP_REQUIRE_SECURE_NATS=1` rejects insecure NATS and default development credentials, and the matchmaker/control entrypoint fails fast for production NATS setups without TLS unless explicitly allowed for temporary testing.
- Added original Powerline crown and sound assets to the client. Rank-one players now render a crown above their head and on the minimap. The client loads sliced crash, food-grab, line-loop, and fast-line-loop OGG clips through Bevy's asset system; confirmed deaths and local confirmed food pickups trigger one-shot sounds, while speed audio uses persistent loop entities whose volume changes instead of restarting during rollback/correction.
- Added location-aware remote-player sound. A Bevy `SpatialListener` follows the local snake head; remote confirmed death/food one-shots and remote speed loops use the remote snake head as source position, apply configurable full/max distance attenuation, and keep existing loop entities alive while only changing volume/position. This stays on Bevy's built-in audio stack for now; `bevy_seedling` is not needed until we want more advanced mixing/effects.
- Documented the hybrid Edgegap test path where the game server runs as a real Edgegap session while the matchmaker and web client run locally. This path does not need Edgegap's managed matchmaker, but it does need a NATS endpoint reachable by both the local matchmaker and Edgegap game-server container.
- Built and pushed the latest Bevygap-enabled game-server image as `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-server:webtest-20260529-110706`.
- Set the production netcode protocol id to `1`, generated a 64-hex-character `LIGHTRIDER_PRIVATE_KEY`, and stored both in ignored `secrets/prod-netcode.env`.
- Fixed the matchmaker image build recipe so empty no-cache args do not trip `set -u`, exposed optional Podman build resource kwargs, and exposed opt-in Docker build args for release incremental compilation.
- Fixed Edgegap app-version automation against the live API: removed unsupported `build_type`, disabled forced image cache by default, stopped sending `req_cpu`/`req_memory` on PATCH updates, ignored Edgegap-normalized `verify_image`/port TLS defaults, normalized `session_config` ordering, and synced/verified app version `lightrider/webtest-20260529-110706` with server image tag `webtest-20260529-110706`.
- Added cargo-chef to the production server and matchmaker Dockerfiles, including separate Bevygap and Lightrider web recipes for the matchmaker/control image, and added a release-only GitHub Actions workflow that pushes both images to the Edgegap registry.
- Verification for the cargo-chef/release workflow slice: GitHub workflow YAML parses with Ruby, `just --dry-run edgegap-build testtag`, `just --dry-run matchmaker-build testtag`, and `git diff --check` pass. `podman build --target planner` passes for both `Dockerfile.server` and `Dockerfile.matchmaker`, proving cargo-chef can prepare recipes over the staged `lightrider`/`bevygap`/`lightyear` context without running full release compilation.
- Clarified the release image path: GitHub Actions uses the Lightrider release ref and falls back Lightyear/Bevygap to `main`, with optional `LIGHTYEAR_REF`/`BEVYGAP_REF` overrides and optional `CHECKOUT_TOKEN` for private dependency checkout. Added one-line release/deploy helpers: `just github-release <tag>`, `just prod-images-build-push <tag> memory=24g cpus=8`, `just deploy-web-server-pull <vps-ip> <tag>`, and `just edgegap-release-sync <tag> <nats-host>:4222`. Verification: workflow YAML parses, `just --summary`, release/deploy dry-runs, top-level production-image kwarg/positional dry-runs, and `git diff --check` pass.
- Real `just prod-images-build dev memory=24g cpus=8` testing found the server cargo-chef cook failed because external path deps from `../../bevygap` were not present in the builder cook stage. The Dockerfiles now copy sibling `lightyear`/`bevygap` before cook stages so Cargo can resolve those path dependencies; this trades some sibling-source cache invalidation for correctness.
- Real matchmaker image testing then failed at `rustup target add wasm32-unknown-unknown` because the local container did not trust the enterprise TLS root used for `static.rust-lang.org`. `edgegap-context` and the GitHub workflow now stage `host-ca-certificates.crt`; the Docker chef stages append it to the container CA bundle and set `RUSTUP_USE_CURL=1`/CA env vars so local and CI builds can download toolchain components through the trusted bundle.
- The next matchmaker image failure showed that `cargo chef cook` can leave cooked/skeletonized path-workspace files in place; the final build then saw `bevygap_client_plugin` as a dummy `0.0.1` package without the real API. Both production Dockerfiles now restore full `lightyear` and `bevygap` sources again before final app builds.
- Verification after these fixes: `just prod-images-build dev memory=24g cpus=8` completed successfully for both production images, then `just prod-images-push dev memory=24g cpus=8` pushed `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-server:dev` and `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-matchmaker:dev`.
- Added explicit `ssh_key=...` support to `deploy-web-server-pull`, `deploy-web-server`, and `web-server-install` because the VPS install path otherwise uses plain `ssh root@host`/`scp root@host` and prompts repeatedly when the key is not loaded by default.
- First VPS install of `lightrider-matchmaker:dev` pulled successfully but the service restarted immediately. Local reproduction showed the runtime image's Debian nginx default site attempted to bind `[::]:80`; IPv6 is disabled in this Podman/container environment, so nginx exited before serving the app. The matchmaker entrypoint now removes the default nginx site before writing the Lightrider config, and prints internal NATS/matchmaker/httpd/nginx logs when startup exits nonzero. The host setup script now keeps failed containers instead of `--rm` and prints recent systemd logs when local health checks fail. Verification: `bash -n` for both scripts, rebuilt/pushed `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-matchmaker:dev`, and local container smoke on alternate ports (`WEB_PORT=18080`, `NATS_PORT=14222`, `NATS_MONITOR_PORT=18222`, `MATCHMAKER_HTTPD_PORT=13000`) passed.
- Fixed another VPS startup footgun: `web-server-env-template` used to preserve an existing `NATS_PASSWORD=lightrider`, which made production startup refuse default NATS credentials. The template now treats unset or default `lightrider` passwords as unsafe and generates a random password even when overwriting an existing env file.
- Fixed the next production wiring issue: the matchmaker was configured with `EDGEGAP_APP_VERSION=dev`, but Edgegap returned 404 because app version `lightrider/dev` did not exist and the organization was already at its two-version limit. `edgegap-release-sync`, `deploy-web-server`, `deploy-web-server-pull`, and `web-server-env-template` now support a separate `edgegap_version=...`/version argument so Docker tag `dev` can be deployed through an existing Edgegap app version such as `webtest-20260529-110706`. Synced and verified `lightrider/webtest-20260529-110706` to server image tag `dev` with the current VPS NATS settings.
- Hardened local Bevygap matchmaker HTTPD startup: NATS connection/listener/server setup errors now return contextual errors instead of panicking on `unwrap`, and local Lightrider just recipes source `secrets/nats.env`/`secrets/prod-netcode.env` when present. For the real Edgegap/Linode flow, both `bevygap-matchmaker-local` and `bevygap-matchmaker-httpd-local` must use the Linode NATS host rather than the local default `127.0.0.1:4222`.
- Fixed the Edgegap ready-vs-cert-digest race in Bevygap matchmaker. Session creation can report `Ready` before the game server's NATS KV cert-digest write is visible, so `bevygap_matchmaker` now polls cert-digest keys for up to `--cert-digest-timeout-ms` (default 15000, poll default 200ms) before failing. Lightrider local recipes and the matchmaker/control entrypoint expose this as `BEVYGAP_CERT_DIGEST_LOOKUP_TIMEOUT_MS` and `BEVYGAP_CERT_DIGEST_LOOKUP_POLL_MS`.
- Verification for the Bevygap P0 slice: `cargo check --manifest-path ../bevygap/Cargo.toml -p bevygap_server_plugin -p bevygap_shared -j 2`, `cargo check -p server --features bevygap -j 2`, `bash -n tools/matchmaker-container-entrypoint.sh`, `git diff --check` in Lightrider and Bevygap, entrypoint fail-fast probes, and `just bevygap-local-smoke 8 config/test.ron 7777 3000 9876 3001` passed. The latest passing smoke logs are under `logs/bevygap/20260528-133903`.
- Added `tools/edgegap_app_version.sh` and `just` wrappers for Edgegap app-version automation. The script can print desired state, show current Edgegap state, redacted-diff desired vs current, sync by creating/updating the app version, and verify deploy-critical fields. It manages server image/tag, UDP/7777 game port, Bevygap session config, required Lightrider/NATS env vars, optional registry credentials, and writes a redacted `.edgegap-build/edgegap-app-version.json` manifest.
- Fixed the three Bevygap server-side gaps that were not blocked by public NATS: `bevygap_server_plugin` now supports `BEVYGAP_CONTEXT_MODE=local` for in-process local Edgegap context, WebTransport cert digests are published and resolved by deployment request id plus endpoint `ip:port` with legacy public-IP fallback, and the local mock flow now verifies active connection reporting through both the server KV write and matchmaker active-connection watcher. Verification: `cargo check -j 2 -p bevygap_server_plugin -p bevygap_matchmaker`, `cargo test -j 2 -p bevygap_shared --features nats`, `cargo test -j 2 -p bevygap_server_plugin local_context_contains_expected_endpoint`, `just --dry-run` for affected Bevygap recipes, and `just bevygap-local-smoke 3 config/test.ron 7777 3000 9876 3001` passed with logs under `logs/bevygap/20260528-144045`.
- Improved Bevygap P1 items 2-7: added `BEVYGAP_NATS_NAMESPACE` and shared subject/bucket helpers, exposed Bevygap TTL envs, routed matchmaker request subjects through shared helpers, hardened server-plugin NATS connect/watch/event handling, converted matchmaker/HTTPD runtime panics into logged or streamed errors, added unit tests for namespace naming, updated local/prod recipes and Edgegap app-version automation, and tightened the Edgegap OpenAPI client regeneration script. Verification: `cargo test -j 2 -p bevygap_shared --features nats`, `cargo check -j 2 -p bevygap_server_plugin -p bevygap_matchmaker -p bevygap_matchmaker_httpd`, script `bash -n` checks, `just --dry-run` for affected recipes, `cargo test -j 2 -p bevygap_server_plugin local_context_contains_expected_endpoint`, `git diff --check`, and `just bevygap-local-smoke 3 config/test.ron 7777 3000 9876 3001` passed with logs under `logs/bevygap/20260528-150923`.
- Started the browser UI implementation. Added the `web_client` workspace crate using Leptos CSR plus `leptos-bevy-canvas`, moved the `lightrider-web` WASM binary out of the native client crate, mounted Bevy on a fixed canvas selector, added the Powerline-style join modal, player-name field, four-letter private room field, and URL-driven browser settings. Added `RoomCode` and `RoomJoinMode::Private`, made server room assignment map codes to stable private rooms, and fixed public auto-join so it ignores private rooms. Updated `Dockerfile.matchmaker` to build `-p web_client --bin lightrider-web`. Verification: `cargo fmt --all`, `cargo test -j 2 -p shared --lib room_codes`, `cargo test -j 2 -p client --lib rooms`, rustup cargo wasm check for `web_client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `cargo check -j 2 -p client --features bevygap`, `cargo test -j 2 -p server --lib auto_room_does_not_assign_private_rooms`, and `cargo test -j 2 -p server --lib private_room_does_not_fall_back_to_public_when_room_limit_is_full`.
- Added multi-room Bevygap deployment packing. `RequestSession` now carries optional room intent, `bevygap_client_plugin` sends it, HTTPD forwards the full request, Lightrider clients map `RoomJoinMode` into Bevygap `RoomSelection`, game servers publish `BevygapDeploymentMetrics` from `RoomDirectory`, and `bevygap_matchmaker` reads deployment metrics from NATS KV to decide whether to reuse a warm Edgegap deployment or create a new one. Verification: `cargo test -j 2 --manifest-path ../bevygap/Cargo.toml -p bevygap_shared`, `cargo test -j 2 --manifest-path ../bevygap/Cargo.toml -p bevygap_matchmaker`, `cargo check -j 2 --manifest-path ../bevygap/Cargo.toml -p bevygap_matchmaker -p bevygap_matchmaker_httpd -p bevygap_client_plugin -p bevygap_server_plugin`, `cargo test -j 2 -p shared --lib room_codes`, `cargo test -j 2 -p server --features bevygap room_metrics`, `cargo check -j 2 -p client --features bevygap`, and wasm `cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`.
- Added web-server/control-host automation. `tools/setup_web_server_host.sh` installs Podman, pulls the matchmaker/control image, writes a systemd service, persists NATS data, and exposes web/NATS ports. `just deploy-web-server host=<vps-ip>` builds/pushes the image, writes/refreshes an ignored shell env file from local Edgegap/netcode secrets, copies the env and script to the VPS, runs setup, removes the temporary uploaded env file, and health-checks the web endpoint. Verification: `bash -n tools/setup_web_server_host.sh`, `just --dry-run deploy-web-server host=45.79.138.102`, `just --dry-run web-server-env-template`, `just --dry-run web-server-install`, and `git diff --check`.
- Tuned the matchmaker/control image build after local `bevygap_matchmaker` release builds were killed by SIGKILL and a later WASM build missed `wasm32-unknown-unknown`. `Dockerfile.matchmaker` now installs the WASM target both before tool installation and after the repo copy, defaults to a 36GB-friendly profile (`jobs=2`, thin LTO, codegen units 8 for native; `jobs=2`, size opt, no LTO for WASM), and still supports low-memory overrides; `SKIP_IMAGE_BUILD=1 just deploy-web-server host=<vps-ip> tag=<tag>` can reinstall an already-pushed image without rebuilding locally, and `NO_CACHE=1 just matchmaker-build <tag>` forces a clean container build if the Rust target/toolchain cache is suspect.
- Added local browser UI test instructions and recipes. `just web-build` installs/checks the WASM target, keeps `wasm-bindgen-cli 0.2.122` under ignored `.edgegap-build/tools`, builds `web_client --features bevygap` with `rustup run nightly cargo` to avoid non-rustup Cargo wrappers missing the WASM sysroot, and writes `web/pkg`; `just web-serve` serves the static browser shell on `localhost:8000` for testing against the local mock Bevygap WebSocket endpoint. Renamed the ambiguous local gateway recipe to `bevygap-matchmaker-httpd-local` and added `bevygap-matchmaker-mock-stack-local` so the mock matchmaker worker and WebSocket gateway can be started together.
- Cleaned up local Bevygap logging and remote-browser docs. Game-server deployment metric writes are now debug logs, matchmaker unclaimed-session age logs use minute/second formatting and the periodic session-age line is debug-level, and `DEPLOYMENT.md` now calls out that SSH `-L` forwards only TCP, not the WebTransport/QUIC game-server UDP port.
- Fixed first browser smoke-test polish issues after a successful real Edgegap join through local matchmaker/control: the web menu now has a close button, private/specific/new room selections show a persistent room badge, and the Bevy client relies on Lightyear's wasm web keepalive to reduce hidden-tab freezing. Also documented that Edgegap CPU metrics are not automatically fed into NATS. Verification: `cargo fmt --all`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `cargo check -j 2 -p client --features bevygap`, and `cargo check -j 2 -p server --features bevygap`.
- Added browser loading/matchmaking status text. Static HTML now shows `Loading assets...` before the WASM bundle initializes; the Bevy WASM client updates that status element from Bevygap state and hides it after Lightyear connects. Bevygap matchmaker now explicitly streams `creating new deployment` when it does not reuse a warm deployment so the browser can show `Waiting for new server to start`; routing to an existing deployment shows `Waiting to connect to server`. Verification: Lightrider `cargo fmt --all --check`, Bevygap `cargo fmt --manifest-path ../bevygap/Cargo.toml --all --check`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `cargo check -j 2 -p client --features bevygap`, `cargo check --manifest-path ../bevygap/Cargo.toml -j 2 -p bevygap_matchmaker`, `just web-build`, and both `git diff --check` commands.
- Added server-side CPU polling to the Bevygap deployment metrics heartbeat. The same game-server event now pushes room/player capacity plus normalized process CPU to NATS, so the matchmaker can apply its existing `max_cpu_percent_per_deployment` policy without polling Edgegap. Verification: `cargo fmt --all` and `cargo test -j 2 -p server --features bevygap bevygap_metrics`.
- Added the first real Powerline asset-backed render path. `assets/powerline/sheet.png` is copied from the original game, `render.use_assets` selects sprites vs gizmos, and Bevy `Sprite` source rects render the grid background, arena border, snake heads/tails, and food. `just web-build` and `Dockerfile.matchmaker` now copy assets into the static web root, while `web/assets/` stays ignored as generated output. Verification: `cargo fmt --all`, `cargo check -j 2 -p client --features bevygap`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `cargo test -j 2 -p shared --lib config`, `cargo check -j 2 -p server --features bevygap`, and `just web-build`.
- Added an in-game Bevy network debug button and panel. The panel reads the local Lightyear client link stats and displays connection state, ping/RTT, and jitter without relying on Leptos UI. Verification: `cargo fmt --all --check`, `cargo test -j 2 -p client --lib network_debug_stats`, `cargo check -j 2 -p client --features bevygap`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `just web-build`, and `git diff --check`.
- Disabled Bevy asset metadata lookup for rendered clients so web builds do not try to deserialize missing `*.meta` files for `powerline/sheet.png` and sound assets. Verification: `cargo fmt --all --check`, `cargo check -j 2 -p client --features bevygap`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `just web-build`, and `git diff --check`.
- Improved asset-backed visuals after browser testing: the arena background now uses a single tiled sprite to remove grid tile gaps, snake tails render as layered glow/core strips with caps at turns, and the default head size is reduced from `10.0` to `6.0`. Verification: `cargo fmt --all --check`, `cargo check -j 2 -p client --features bevygap`, `cargo test -j 2 -p shared --lib config`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, and `just web-build`.
- Increased default food density and spawn cadence, added asset-backed boost contact effects using original Powerline spark/lightning frames, and added high-speed head particles using the original particle dot frame. Verification: `cargo fmt --all --check`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `cargo test -j 2 -p shared --lib config`, `cargo test -j 2 -p client --lib spark_frames_cycle`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, and `just web-build`.
- Switched snake readability back to collision-first rendering: asset tails are now a single uniform one-pixel line by default, while heads are elongated and rotated in the movement direction. Verification: `cargo fmt --all --check`, `cargo check -j 2 -p client --features bevygap`, `cargo test -j 2 -p shared --lib config`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, and `just web-build`.
- Added secret server-authoritative admin mode. Local secrets now include ignored `secrets/admin.env`; local server recipes source it; the shared protocol has admin login/response/command messages; the server authenticates admin connections and applies per-room `set_num_bots` overrides; the client has a hidden admin unlock/panel that can set the current room's in-game bot target and displays rollback metrics from Lightyear `PredictionMetrics`. Verification: `cargo check -j 2 -p server -p client --features bevygap`, `cargo test -j 2 -p server --lib`, `cargo test -j 2 -p client --lib admin`, `cargo test -j 2 -p shared --lib`, and wasm `cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`.
- Removed Lightrider's direct `bevy_web_keepalive` dependency and plugin insertion because Lightyear already installs `lightyear_web::WebKeepalivePlugin` for wasm clients. This leaves one explicit keepalive path and avoids duplicate hidden-tab worker wakeups. Verification: `cargo check -j 2 -p client --features bevygap`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `just web-build`, `cargo fmt --all --check`, and `git diff --check`.
- Fixed the admin UI startup panic caused by conflicting mutable Bevy `Text` queries in `client::admin::update_admin_view` by moving the four text queries into a `ParamSet`. Added a regression test that runs the system in a minimal Bevy app so Bevy's system-param validator catches future `B0001` conflicts. Verification: `cargo fmt --all --check`, `cargo test -j 2 -p client --lib admin_view_system_runs_without_query_conflicts`, `cargo check -j 2 -p client --features bevygap`, wasm `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, and native rendered smoke `RUST_LOG=warn timeout 20s just local 0 config/test.ron 5018 1 1001 auto`. The smoke ran until timeout without the admin panic; it still logged missing local Powerline asset paths and one large-rollback warning.
- Fixed native `just local` rendering where only UI showed while the arena entities were missing. Bevy was resolving `powerline/sheet.png` and sound assets under `client/assets`, but the repo stores runtime assets under workspace-root `assets`; native clients now set `AssetPlugin.file_path` to `../assets` while wasm keeps `assets`. Verification: `cargo fmt --all --check`, `cargo test -j 2 -p client --lib native_clients_load_assets_from_workspace_root`, `cargo check -j 2 -p client --features bevygap`, wasm `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, native smoke `RUST_LOG=warn timeout 18s just local 0 config/test.ron 5021 1 1001 auto` with no asset path errors, and a captured rendered-window smoke under `logs/runtime-smoke/20260530-220329-asset-root-late` showing arena, food, snakes, minimap, and leaderboard.
- Added location-aware Bevygap static deployment routing. `DeploymentMetrics` now carries provider/country/region metadata; `bevygap_server_plugin` fills it from env; `bevygap_matchmaker` can mint a direct `ConnectToken` for a static deployment instead of creating an Edgegap session; public traffic uses optional MaxMind GeoIP plus `BEVYGAP_STATIC_CLIENT_COUNTRY_CODES`, while private/specific rooms ignore location. The VPS installer can now run a colocated `lightrider-static-server` service on UDP/WebTransport port `7777`. Also cleaned up `bevygap_matchmaker_httpd`'s normal empty-response sentinel log so it only warns when the stream ends before `SessionReady`/`Error`. Verification: Bevygap `cargo fmt --all`, `cargo test -j 2 -p bevygap_matchmaker`, `cargo test -j 2 -p bevygap_shared --features nats`, `cargo check -j 2 -p bevygap_server_plugin -p bevygap_matchmaker_httpd`, Lightrider `cargo check -j 2 -p server --features bevygap`, Lightrider `cargo check -j 2 -p client --features bevygap`, `bash -n` for the control-host scripts, `just --summary`, and `just bevygap-local-smoke 3 config/test.ron 7791 3011 9891 3091` with logs under `logs/bevygap/20260529-163133`.
- Rebuilt and pushed both production images for tag `dev` after the static-routing changes: `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-server:dev` image id `27d1c36f9cf8a470fff81d36b8873655f3a4c5a7d4e3123c4423e8ea6e1e6478` and `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-matchmaker:dev` image id `bf73aa180d4bbd009177540605c61d3ca3a22a1f568b36387365353cf0fac0b7`. Command: `just prod-images-build-push dev memory=24g cpus=8`.
- Diagnosed the real Edgegap browser flow where the matchmaker issued a token and the game server logged the client id as admissible, but no Lightyear request reached the server. The most likely blocker is opening the public control host over plain `http://<vps-ip>`: NATS/session/admission still work, but browser WebTransport requires a secure context and fails before the game server sees a packet. The web shell now preflights `window.isSecureContext`, `WebTransport`, and HTTPS/`wss://` consistency before starting Bevy, so bad browser origins fail before creating Edgegap sessions. Also verified app version `lightrider/webtest-20260529-110706` points to `lightrider-server:dev`, protocol id `1`, NATS namespace `lightrider_webtest-20260529-110706`, and UDP port `7777`.
- Rebuilt and pushed the matchmaker/control image after the browser preflight and deployment bootstrap fixes: `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-matchmaker:dev` image id `d3b76e68192e256fa10234710bce3a693aca1ea355e151a19222130b328cb61d`.
- Added first-class HTTPS deployment support for the VPS control host. `just deploy-web-server` and `just deploy-web-server-pull` now accept `domain=...`; the installer installs/configures Caddy, binds the matchmaker/control container to `127.0.0.1:8080`, sets `MATCHMAKER_CORS=https://<domain>`, and bootstraps the browser with `wss://<domain>/matchmaker/ws`. The same installer now explicitly passes NATS TLS cert/key/CA env vars into the container for later production hardening.
- Added `just web-server-enable-nats-tls-from-caddy` for the `sslip.io`/Caddy path. It copies Caddy's Let's Encrypt certificate/key into `/etc/lightrider`, updates the matchmaker env to run NATS with TLS, points in-container NATS clients at `<domain>:4222`, updates the optional static game-server env the same way, restarts the services, and verifies the public TLS endpoint with `openssl s_client`. With a publicly trusted certificate, Edgegap game servers do not need a custom `NATS_CA`.
- Created and verified Edgegap app version `lightrider/dev` pointing at `lightrider-server:dev` with the current temporary insecure NATS settings. The current deployment flow should use `edgegap_version=dev`; after Caddy/NATS TLS is enabled, resync with `EDGEGAP_NATS_INSECURE=0` and `NATS_HOST=45.79.138.102.sslip.io:4222`.
- Fixed a Bevygap matchmaker Edgegap session-create bug: session requests now include `version_name=settings.app_version`. Without that, Edgegap could create/return a session with `version: null`, which failed generated-client deserialization as `invalid type: null, expected a string` before the matchmaker could send `SessionReady`.
- Diagnosed a deployed `no cert digest found for public_ip` failure. Edgegap game-server logs showed `NATS: TLS is enabled` followed by `received corrupt message of type InvalidContentType`, which means the game server was configured for TLS while the VPS NATS endpoint was still plain TCP. In that state the game server never publishes its WebTransport cert digest, so the matchmaker lookup times out. Fix by either syncing the Edgegap app version with insecure NATS for smoke tests, or by first enabling NATS TLS from Caddy and then syncing the Edgegap app version with `EDGEGAP_NATS_INSECURE=0`.
- Stopped the broken Edgegap deployment `bcb03c5bc925` and resynced app version `lightrider/dev` for the current smoke-test posture: `NATS_HOST=45.79.138.102:4222`, `NATS_INSECURE=1`, `BEVYGAP_REQUIRE_SECURE_NATS=0`, namespace `lightrider_dev`.
- Rebuilt and pushed the game-server image for tag `dev`: `registry.edgegap.com/lightyear-6qgcf4w4mrq7/lightrider-server:dev`, image id `6ac79ca15c2a21c2b014bdd11f6db9652badec1eeda38e7f5987580ba5dbc569`. Command: `just edgegap-build-push dev cpus=2`.
- Stopped stale Edgegap deployments `5c16daeaa58d`, `32d4a4362e68`, and `f2d471954437` while reconciling NATS mode. The VPS NATS endpoint is now TLS with a certificate for `45.79.138.102.sslip.io`, so the final verified app-version state is `NATS_HOST=45.79.138.102.sslip.io:4222`, `BEVYGAP_REQUIRE_SECURE_NATS=1`, no `NATS_INSECURE`, namespace `lightrider_dev`.
- Fixed local admin secret export and added mixed headless rollback instrumentation. `just server`, `just local`, `just trace-local`, `just trace-local-mixed`, and `just bevygap-server-local` source `secrets/admin.env` under `set -a`, so a plain `LIGHTRIDER_ADMIN_PASSWORD=...` file actually reaches the server. Added `FoodConfig.max_count` with default/test RON values and enforced it for timed spawns plus death-food drops. Added `trace-local-mixed` and expanded `tools/debug_trace_summary.sql` with rollback histograms/component counts. Fixed a rollback bug where `update_tails` treated Bevy change detection on `TailPoints` as a turn signal; rollback/restore could mark the component changed and create false extra tail points. Turns now explicitly push inflection points in `turn_tail`, and bots avoid no-op/reverse turn writes. Added rollback tolerances for tiny float drift in snake speed, acceleration, food boost, length, and tail points. Mixed headless trace `logs/debug/20260530-223456-mixed-headless` ran 10 seconds with one headless player, two headless bot clients, and four server bots: 152 rollback requests, mostly 1-2 ticks, with remaining mismatches dominated by food-pickup effects (`Speed`, `Acceleration`, `TailLength`, `FoodBoost`, `TailPoints`). Verification: `just trace-local-mixed 10 config/test.ron 5064 3001 3002 2 auto`, `cargo test -j 2 -p shared --lib`, `cargo test -j 2 -p server --lib death_food_spawn_limit_respects_room_food_capacity`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `cargo fmt --all --check`, `git diff --check`, and an admin server smoke on port `5071`.
- Delayed initial player spawn until the client's input timeline is synced. Native clients now send `RoomJoinRequest` for all room modes, including `auto`, only after `IsSynced<InputTimeline>`; the server spawns the player/snake on that request instead of on raw connection. A mixed headless trace `logs/debug/20260530-225455-mixed-headless` ran 8 seconds with one headless player, two headless bot clients, and four server bots: startup 12-tick rollbacks disappeared, rollback requests dropped to 80 and were all 1-2 ticks, with remaining mismatches still dominated by food-pickup boost/growth (`Speed`, `TailLength`, `FoodBoost`). Verification: `just trace-local-mixed 8 config/test.ron 5065 3001 3002 2 auto`, `cargo test -j 2 -p shared --lib`, `cargo test -j 2 -p server --lib player_ranks_are_computed_per_room_by_score`, `cargo check -j 2 -p client --features bevygap`, and `cargo check -j 2 -p server --features bevygap`.
- Added conservative client-side food pickup prediction for the controlled snake. The client now reads latest confirmed food `Position` history, applies predicted `TailLength` growth plus `FoodBoost` locally, tracks predicted food ids in rollback-managed snake state, and leaves food visuals/despawns fully server-confirmed. Added `FoodConfig.predicted_pickup_radius_scale` to keep this data-driven; default/test configs use `0.5`. Mixed headless trace `logs/debug/20260530-231905-mixed-headless` ran 6 seconds with one headless player, two headless bot clients, and four server bots: no invariant violations or long rollback deltas, but 98 rollback requests remained, dominated by `Speed` plus food growth/boost mismatches. Verification: `cargo test -j 2 -p client --lib predicted_food_pickup`, `cargo test -j 2 -p shared --lib predicted_pickup_radius_is_clamped_to_server_pickup_radius`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `cargo fmt --all --check`, and `just trace-local-mixed 6 config/test.ron 5068 3001 3002 2 auto`.
- Removed authoritative food attraction and changed local food prediction to boost-only. Server food no longer moves toward heads; it is eaten only when the post-movement head is within `food.radius`. The client now uses a rollback-managed `RecentlyBoostedFromFood` `VecDeque` on predicted snakes to avoid duplicate speculative boosts from the same food entity, applies only `FoodBoost`, and no longer predicts tail growth or reads food `ConfirmedHistory` for gameplay. Confirmed `FoodCollision` is converted once into a local `ConfirmedFoodPickup` message so sound and a short sprite pull-in animation can both react. Removed `FoodConfig.predicted_pickup_radius_scale`, `magnet_radius`, and `magnet_speed` from config. Mixed trace `logs/debug/20260531-111302-mixed-headless` no longer shows a `FoodBoost` mismatch bucket; remaining rollbacks are dominated by `Speed`, plus a known short predicted boundary-death window. Verification: `cargo test -j 2 -p client --lib predicted_food_boost`, `cargo test -j 2 -p server --lib food`, `cargo test -j 2 -p shared --lib config`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `cargo fmt --all --check`, `git diff --check`, and `just trace-local-mixed 4 config/test.ron 5071 3001 3002 2 auto`.
- Ran a no-server-bots trace with one autonomous headless client to isolate baseline predicted movement while still taking turns. Temporary trace config disabled server bots and used a larger arena; `logs/debug/20260531-111750-solo-bot-no-server-bots` ran 8 seconds with one `--mode bot` client, zero invariant violations, one rollback request, one `TailPoints` mismatch, and zero `Speed`/`FoodBoost`/`TailLength` mismatches. This suggests the current mixed-trace `Speed` corrections are not caused by local turning alone.
- Made input-delay defaults explicitly use the Lightyear balanced preset (`minimum_input_delay_ticks=0`, `maximum_input_delay_before_prediction_ticks=3`, `maximum_predicted_ticks=7`) and documented the preset in `config/default.ron` and `config/test.ron`. Fixed proximity boost prediction so simulated/predicted snakes can boost against interpolated remote tails without simulating those remote snakes. Mixed headless trace `logs/debug/20260531-124457-mixed-headless` ran 4 seconds with one headless player, two headless bot clients, and four server bots: rollback requests dropped to 5 and there were zero `Speed`/`FoodBoost` mismatches; remaining mismatches were `TailPoints=3` and `TailLength=2`, plus the known short predicted boundary-death window. Verification: `cargo test -j 2 -p shared --lib collision::collider`, `cargo test -j 2 -p shared --lib config`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `cargo fmt --all --check`, `git diff --check`, and `just trace-local-mixed 4 config/test.ron 5074 3001 3002 2 auto`.
- Restored robust confirmed food pickup animation and adjusted feel. `FoodCollision` now carries confirmed food/head positions, so the client can spawn the pull-in animation even if the replicated food entity has already despawned locally. The default/test pickup radius is now `45.0`, close to the old `75.0` attraction radius without reintroducing authoritative food motion. Normal camera zoom growth is slower (`0.00025` per tail length) and smoothed with `normal_camera_scale_smoothing=6.0`. Server snake collision now sweeps from previous to current head position using actual speed, with a regression test for high-speed tunneling through a line. Mixed headless trace `logs/debug/20260531-132927-mixed-headless` ran 4 seconds with one headless player, two headless bot clients, and four server bots: 11 rollback requests, all 2 ticks, zero invariant violations, with remaining mismatch buckets from large-radius food pickup contention (`Speed=5`, `TailLength=5`, `FoodBoost=4`, `Acceleration=3`, `TailPoints=3`). Verification: `cargo test -j 2 -p server --lib food`, `cargo test -j 2 -p server --lib collision::collider`, `cargo test -j 2 -p client --lib camera`, `cargo test -j 2 -p shared --lib config`, `cargo test -j 2 -p client --lib food`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, and `just trace-local-mixed 4 config/test.ron 5076 3001 3002 2 auto`.
- Replaced the asset snake tail's stretched wall texture and joint dots with solid luminous trail layers: a one-pixel collision-readable core plus softer inner/outer glow strips. The 2D camera now enables Bevy bloom/tonemapping so HDR tail/head colors bloom on supported renderers, while the layered sprites still provide a visible glow fallback. Verification: `cargo fmt --all --check`, `cargo check -j 2 -p client --features bevygap`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, and local rendered smoke `RUST_LOG=warn timeout 12s just local 0 config/test.ron 5082 1 1001 auto` with no rendering panic.
- Added name-hashed player colors and fixed confirmed food pickup presentation. Clients now use a ten-color snake palette selected by stable case-insensitive name hash; snake cores/glows/heads, name labels, and boost effects are tinted from that palette. Tail glow was softened by widening the low-alpha glow layers while keeping the one-pixel core. Food collisions are now sent from the server to clients in the same room, so confirmed pickups trigger the pull-in animation and food sound; the animation tracks the live snake head when the entity is mapped and falls back to the confirmed head position when early messages arrive before entity mapping. Food boost is more gradual (`food_boost_acceleration=0.03`, `food_boost_decay=0.94`). Boost sparks now sit on the contacted tail edge, while the lightning strip only appears near top speed. Mixed headless trace `logs/debug/20260531-140006-mixed-headless` ran 4 seconds with zero invariant violations and 9 small rollback requests (8x 2 ticks, 1x 3 ticks); the trace still showed a few early `entity_map_missing` entries for food collisions, but the fallback positions handled them without panic. Verification: `cargo test -j 2 -p client --lib snake_color_hash`, `cargo test -j 2 -p shared --lib food_boost`, `cargo test -j 2 -p client --lib food`, `cargo test -j 2 -p client --lib top_speed_marker_threshold`, `cargo test -j 2 -p server --lib food`, `cargo test -j 2 -p shared --lib config`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `just trace-local-mixed 4 config/test.ron 5085 3001 3002 2 auto`, and rendered smoke `RUST_LOG=warn timeout 12s just local 0 config/test.ron 5086 1 1001 auto`.
- Clarified collision-vs-visual behavior and adjusted the snake glow/boost marker. Server death collision remains centerline-only: it does not read render tail width or glow size, and a new regression test confirms a snake can pass inside the visual glow envelope without dying when it does not cross the core segment. The glow renderer now uses several very low-alpha bands instead of two hard rectangles, trims glow away from the head on the front segment, and keeps only the core line reaching the head. Boost sparks/lightning now originate from the core-line hit point rather than the visual glow edge. Verification: `cargo fmt --all --check`, `cargo test -j 2 -p server --lib near_miss_inside_visual_glow_does_not_collide`, `cargo test -j 2 -p server --lib collision::collider`, `cargo test -j 2 -p client --lib top_speed_marker_threshold`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, and rendered smoke `RUST_LOG=warn timeout 12s just local 0 config/test.ron 5088 1 1001 auto`.
- Replaced asset snake tail rectangles with generated 2D mesh capsules and rounded join circles. The core remains a thin solid tube while soft transparent capsule layers plus HDR bloom provide the glow; this avoids square sprite seams and gives turns a rounded visual join without changing collision geometry. Normal camera max zoom is capped lower (`0.85`). Death food now samples the full tail length with small deterministic jitter and higher caps (`default=120`, `test=60`), and food sprites get deterministic rotation. Confirmed deaths are sent to clients in the same room and spawn a separate visual snapshot that flashes white and collapses toward the tail midpoint after the replicated snake is removed. Verification: `cargo fmt --all --check`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `cargo test -j 2 -p server --lib death_food`, `cargo test -j 2 -p shared --lib config`, wasm `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `git diff --check`, and rendered smoke `DISPLAY=${DISPLAY:-:0} RUST_LOG=warn timeout 18s just local 0 config/test.ron 5094 1 1001 auto`, which ran until the intentional timeout without render/system panics.
- Removed the per-pickup `Food collision` server info log. Lowered the normal camera max zoom again to `0.62`. Simplified asset tails to only the bright core mesh and increased Bevy bloom slightly, removing the explicit transparent glow layers that caused visible blobs at turns. Cached the local player's last tail snapshot before death handling so the local death flash/collapse can still play if the predicted snake has already despawned. Added a minimap trail renderer for the local player's full snake path. Reduced and centered the proximity-boost spark marker against the contacted core line, and replaced the old head-adjacent petal effect with small fading particles emitted behind the head as speed rises. Verification: `cargo fmt --all --check`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `cargo test -j 2 -p shared --lib config`, `cargo test -j 2 -p client --lib top_speed_marker_threshold`, wasm `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `git diff --check`, and rendered smoke `DISPLAY=${DISPLAY:-:0} RUST_LOG=warn timeout 45s just local 0 config/test.ron 5098 1 1001 auto`, which required a targeted `cargo clean -p client` to clear stale native incremental linker artifacts and then ran until the intentional timeout without render/system panics.
- Polished asset rendering and audio after browser feedback. Snake core materials now render opaque when alpha is 1.0, so thresholded bloom colors the glow from each snake instead of producing black blend fringes; the camera bloom prefilter now avoids blurring the whole map. The arena background is a solid dark-blue rectangle with a uniform blue out-of-bounds clear color, and the old green/wall-stretch outline is replaced by a blue capsule border. Proximity boost effects now require parallel contacted trails: lightning appears when there is enough visible space, while the spark marker remains top-speed gated and sits on the contacted snake core edge facing the local head. Speed particles were reduced in count, enlarged, and switched back to the original `particleDot` sprite. Sound defaults are louder and fully data-driven in RON. Also fixed the local `lightyear_matchmaker_bevy_client` wasm dependency split so web builds do not pull native `mio`/Tokio net into `wasm32-unknown-unknown`. Verification: `cargo fmt --all --check`, `cargo fmt --manifest-path ../lightyear-matchmaker/Cargo.toml --all --check`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `cargo test -j 2 -p shared --lib config`, `cargo test -j 2 -p client --lib top_speed_marker_threshold`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `just web-build`, `git diff --check`, and rendered smoke `DISPLAY=${DISPLAY:-:0} RUST_LOG=warn timeout 25s just local 0 config/test.ron 5100 1 1001 auto`, which ran until the intentional timeout without render/system panics.
- Iterated on render readability. The arena now has a procedural grid again without tiled-PNG seams, plus a low-alpha HDR glow behind the blue border. Snake heads are smaller and rounder, using the original head-dot sprite as a white ball with a colored glow mesh behind it. Player name labels are smaller and closer to the head. Confirmed death visuals were simplified to a quick white in-place fade, with a fallback circle at the confirmed death position if the killed snake snapshot is already unavailable. Proximity lightning now targets the contacted tail core, while the spark marker remains on the near edge and is also shown when the gap is too tight for lightning. Verification: `cargo fmt --all --check`, `cargo check -j 2 -p client --features bevygap`, `cargo check -j 2 -p server --features bevygap`, `cargo test -j 2 -p shared --lib config`, `cargo test -j 2 -p client --lib top_speed_marker_threshold`, `cargo test -j 2 -p client --lib death_animation_spawns_position_fallback_without_tail_snapshot`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features bevygap`, `just web-build`, `git diff --check`, and rendered smoke `DISPLAY=${DISPLAY:-:0} RUST_LOG=warn timeout 25s just local 0 config/test.ron 5101 1 1001 auto`, which ran until the intentional timeout without render/system panics.
- Investigated stutter and "invisible death" reports with headless traces. Added `config/debug_no_food.ron` and `config/debug_solo.ron` for repeatable baseline prediction traces. Fixed a real server CPU bug: headless server builds now use `ScheduleRunnerPlugin::run_loop(1/60s)` like headless clients instead of spinning `MinimalPlugins` as fast as possible; sampled CPU in a mixed headless stack dropped from ~199% to ~18% for the server. Added `server_snake_collision` JSONL debug rows containing the killed head sweep and hit segment for future death correlation. Trace results: `logs/debug/20260531-162912-mixed-headless` no-food/no-bot had one 2-tick startup rollback; `logs/debug/20260531-162945-mixed-headless` food/no-bot had four small rollbacks dominated by `TailLength`; `logs/debug/20260531-163421-mixed-headless` mixed player/two fake-client bots/four server bots had 50 rollbacks, all 1-3 ticks, dominated by food growth/boost mismatches. Verification: `cargo test -j 2 -p server collision::collider -- --nocapture`, `cargo check -j 2 -p server --bin lightrider-server`, `just trace-local-mixed 20 config/debug_no_food.ron 5111 3101 3102 0 auto`, `just trace-local-mixed 20 config/debug_solo.ron 5112 3201 3202 0 auto`, and `just trace-local-mixed 12 config/test.ron 5115 3501 3502 2 auto`.
- Tightened the latest visual polish pass. The proximity spark marker now uses the exact same contacted core point as the lightning anchor instead of applying an edge offset. Arena border glow is softer and more blue, with a lower-alpha glow band behind the bright core. Snake heads are smaller, and the colored head glow has lower alpha for a softer halo. Local confirmed deaths now prefer the cached local tail snapshot, fall back to the cached head position, and render the white fade above normal snake visuals with a small head flash, making the current player's death visible even if the predicted snake has already despawned. Verification: `cargo test -j 2 -p client --lib` and `cargo check -j 2 -p client --features lightyear-matchmaker`; rendered smoke was deferred because a concurrent Podman image build was already running in the repo.
- Follow-up visual fixes after manual feedback showed the previous pass was still not sufficient. The boost spark sprite is now bottom-anchored at the exact same contacted core point that anchors the lightning, so its visible base should line up with the lightning start instead of being centered across the gap. Snake heads now use a smaller white head-dot and a larger tinted head-dot halo instead of a hard-edged mesh circle. Local death rendering now caches the controlled predicted snake tail directly before falling back through `Player.snake`, prefers that cached local snapshot for local deaths, and draws a wider/slower white fade so the current player's own death should remain visible after predicted despawn. The arena edge now uses a thinner blue core plus one wider, lower-alpha blue glow band. Verification: `cargo test -j 2 -p client --lib`, `cargo check -j 2 -p client --features lightyear-matchmaker`, `cargo fmt --all --check`, `git diff --check`, and rendered smoke `DISPLAY=${DISPLAY:-:0} RUST_LOG=warn timeout 20s just local 0 config/test.ron 5120 1 1001 auto`, which ran until the intentional timeout without render/system panics.
- Fixed the local release recipe footgun and reduced the map-edge halo. `just local release=true` now treats the first `release=true` token as the release flag instead of trying to parse it as the bot count; old positional usage and trailing `release=true` still work. The arena border halo was reduced from a very broad glow band to a subtle snake-like accent (`outline_width * 4.5`, minimum 10px, alpha 0.045). Added `rollback_controlled_speed_buckets` to `tools/debug_trace_summary.sql` so future trace summaries can correlate stutter reports with local controlled-snake speed, especially near max speed. The current clue is plausible: at max speed, the same small rollback/correction is more visible because the head moves farther per tick, and max speed is often reached through food/proximity boost paths. Verification: `just --dry-run local release=true`, `just --dry-run local 0 config/test.ron 5121 1 1001 auto release=true`, a shell parser check under `set -u`, `duckdb` trace-summary parse against `logs/debug/latest-mixed/*.ndjson`, `cargo test -j 2 -p client --lib`, `cargo check -j 2 -p client --features lightyear-matchmaker`, `cargo fmt --all --check`, `git diff --check`, and rendered smoke `DISPLAY=${DISPLAY:-:0} RUST_LOG=warn timeout 15s just local 0 config/test.ron 5122 1 1001 auto`, which reached the client and ended by intentional timeout.
- Enabled predicted-snake visual smoothing. Predicted snakes now get `FrameInterpolate<TailPoints>` and `FrameInterpolate<TailLength>`, and the protocol registers visual-correction functions for both predicted components. `TailLength` uses a simple numeric correction delta; `TailPoints` uses a custom per-point offset correction that only smooths when tail topology and directions match, so topology-changing rollbacks still snap rather than smearing an invalid path. A mixed headless trace `logs/debug/20260531-181802-mixed-headless` ran without invariant violations, showed `previous_visual_stored`, `visual_correction_created`, `visual_correction_apply`, and `visual_correction_removed` events, and reported 7 total 2-tick rollback requests. Verification: `cargo test -j 2 -p shared --lib`, `cargo test -j 2 -p client --lib`, `cargo check -j 2 -p client --features lightyear-matchmaker`, `cargo check -j 2 -p server --features lightyear-matchmaker`, `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features lightyear-matchmaker`, `cargo fmt --all --check`, `git diff --check`, and `just trace-local-mixed 4 config/test.ron 5123 3601 3602 2 auto`.
- Disabled predicted visual correction by default while keeping the correction code and tests in place. The protocol no longer registers the `TailPoints`/`TailLength` correction functions, so predicted snakes still get frame interpolation but not correction-smoothing components. Collision death logging was also lowered from `info` to `debug` so normal server logs are less noisy. Remote snake tails continue to use Lightrider's custom length-aware interpolation path; predicted frame interpolation uses Lightyear's per-component frame interpolation with Lightrider's registered `TailPoints`/`TailLength` interpolation functions.
- Fixed several local-play polish and validation issues. Added the missing original Powerline `electroloop` clip and wired a proximity-boost loop for local and spatial remote snakes; added a low-volume background loop using the existing line-loop clip. Name labels now resolve tails from rendered snake entities instead of relying only on `Player.snake`, which makes labels follow predicted/interpolated visuals more reliably. The minimap root marker is now on the actual minimap panel, and the minimap draws per-player tail trajectories for the local room. `just local` now defaults to `LIGHTRIDER_NETWORK_CONDITIONER=average`, which applies Lightyear's average receive-side conditioner on both server links and clients; custom config fields still support explicit one-way artificial latency/jitter/loss. Same-tick reciprocal snake collisions now reserve the first death and ignore later events where the killer was already killed, preventing both snakes from dying from the same collision batch. Verification: `cargo test -j 2 -p shared --lib`, `cargo test -j 2 -p client --lib`, `cargo test -j 2 -p server --lib reciprocal_same_tick_collisions_do_not_kill_both_snakes`, `cargo check -j 2 -p server --features lightyear-matchmaker`, `cargo check -j 2 -p client --features lightyear-matchmaker`, wasm `rustup run nightly cargo check -j 2 -p web_client --target wasm32-unknown-unknown --features lightyear-matchmaker`, `just --dry-run local`, `just --dry-run local release=true`, `cargo fmt --all --check`, `git diff --check`, and rendered smoke `DISPLAY=${DISPLAY:-:0} RUST_LOG=warn timeout 30s just local 1 config/test.ron 5127 1 1001 auto`, which reached runtime and ended by intentional timeout after a targeted `cargo clean -p client` cleared stale native linker artifacts.
- Polished the latest visual/audio feel. Food pickup animations now preserve the same tint as the original food and idle food has a very subtle scale pulse. Base snake speed is slower (`min_speed=0.85`), normal camera zoom grows more slowly (`normal_camera_growth_per_tail_length=0.00015`), and the default soundtrack is disabled while local/nearby food, death, and speed/proximity sounds remain active. HUD/admin/help panels now share a blue transparent style with rounded corners, cyan text glow, blue borders, and soft panel shadows. The arena border core is HDR blue and bloom-enabled, with a slightly stronger but still narrow blue glow band. Local confirmed death now adds a short, low-amplitude camera shake.
- Fixed false death detection after lowering base speed below one pixel per tick. Server snake collision now sweeps only the actual movement distance for the current tick, minus the tiny self-hit epsilon, instead of forcing every sweep to at least `COLLISION_DISTANCE`. This prevents a slow turn from starting the sweep behind the real previous head position and intersecting the snake's own corner. Added regressions for a slow turn not colliding with its own corner and for a slow snake still colliding with a line it actually crosses.
- Added the first dedicated load-test workflow. `config/load.ron` supports 100+ clients in a room with server bots/sound disabled and high food pressure. `just load-local` starts server plus many headless bot clients, captures OS CPU/memory/network samples, writes low-rate Lightyear/perf traces for the server and selected clients, and summarizes frame time, link RTT/jitter, transport bytes, received packet fragments per frame, rollbacks, and log growth. `just fake-clients` attaches many headless bot clients to an existing server, and `just load-summary` reruns the Python summary on any `logs/load/*` directory.
- Replaced the `PlayerInput`/`SpawnPlayer` Lightyear input action with a reliable `PlayerSpawnRequest` message. Initial room join still spawns the first snake after timeline sync; later human respawns send Enter/Space messages and bot/fake clients auto-retry reliable respawn requests. This removes the extra per-tick input stream from load tests, leaving only `SnakeInput` as a Lightyear input. Verification: `cargo fmt --all --check`, `cargo check -j 2 -p server -p client`, `cargo test -j 2 -p server --lib`, `cargo test -j 2 -p client --lib`, `just load-local clients=5 seconds=5 release=false port=5167 trace_clients=1 ramp_per_second=10 net_interfaces=lo`, and a trace/log grep confirming no `PlayerInput`/`SpawnPlayer` rows.
- Added the first broad-phase spatial index implementation. `shared::spatial` now contains room-scoped vertical/horizontal 1D tail segment grids plus a deterministic food KD-tree. Server death collision and shared proximity boost query the tail index before running the existing exact `ray_segment_intersection` checks; server food pickup and client predicted food boost query the food KD-tree. Brute-force reference helpers remain in tests for tail collision, proximity, and food pickup parity. Verification: `cargo test -j 2 -p shared --lib spatial`, `cargo test -j 2 -p shared --lib collision::collider`, `cargo test -j 2 -p server --lib`, `cargo test -j 2 -p client --lib`, `cargo check -j 2 -p server -p client`, `cargo fmt --all --check`, and `git diff --check`.
- 2026-06-01 post-spatial-index 50-client load run: `just load-local clients=50 seconds=20 release=false port=5168 trace_clients=2 ramp_per_second=25 net_interfaces=lo` wrote `logs/load/20260601-122318-local-c50`. Compared with the pre-index dev run, server frame time improved a lot (`avg_frame=69.58ms`, `peak_frame=173.44ms`, `avg_fps=23.6` vs prior `168.71ms`/`481.59ms`/`16.9fps`) and prediction rollbacks improved (`5`, avg delta `2.40`, max `6` vs prior `17`, avg delta `12.12`, max `21`). Server CPU did not improve (`avg_cpu=155.7%`, peak `206.8%`, prior `147.7%`/`197.8%`), and loopback bandwidth/fragmentation got worse (`100.9Mbps` avg, `188.8Mbps` peak, max `32` fragments/frame vs prior `42Mbps` avg, `100Mbps` peak, max `16`). Current read: broad-phase is no longer the obvious limiter in this scenario; replication payload size, trace overhead, transport fragmentation, and per-client fanout are now more suspicious than collision math.
- 2026-06-01 no-JSONL 50-client load run: `just load-local clients=50 seconds=20 release=false port=5169 trace_clients=0 server_trace=false ramp_per_second=25 net_interfaces=lo` wrote `logs/load/20260601-122917-local-c50`. Run directory size dropped from `1.8G` with server+2 traced clients to `292K`. Without trace logging, server CPU stayed roughly similar (`avg_cpu=153.0%`, peak `247.6%`) while aggregate client CPU rose (`peak_cpu=1109.5%`) and loopback traffic rose sharply (`avg_rx/tx=313.4Mbps`, peak `653.2Mbps`). No frame/rollback/fragment metrics are available because those currently come from JSONL trace rows. Interpretation: trace logging was huge for disk and slowed traced clients, but the game can send much more traffic when untraced clients run freely; add a lightweight aggregate metrics sink if we want frame/rollback/fragment summaries without per-packet JSONL.
- `tools/load_summary.py` now separates directional Lightyear packet bandwidth for traced processes using `packet_send` and `packet_recv` rows. Interface `lo` RX/TX remains only aggregate local loopback traffic; the directional trace section answers server/client sent vs received bytes. Verified with `just load-local clients=2 seconds=2 release=false port=5170 trace_clients=1 server_trace=true ramp_per_second=10 net_interfaces=lo`.
- 2026-06-01 50-client load run after setting server replication to 10 Hz: `just load-local clients=50 seconds=20 release=false port=5175 trace_clients=2 server_trace=true ramp_per_second=25 net_interfaces=lo` wrote `logs/load/20260601-215822-local-c50`. Compared with the previous traced 50-client post-index run, loopback bandwidth only dropped slightly (`avg_rx/tx=94.49Mbps`, peak `165.70Mbps` vs `100.9Mbps`, peak `188.8Mbps`), while server CPU stayed similar (`avg_cpu=152.5%`, peak `202.2%`), server frame time was slightly worse (`avg_frame=74.84ms`, peak `180.92ms`, `25.3fps`), and rollbacks were still small but not lower (`8`, avg delta `4.75`, max `7`). The traced server sent `273396.3KiB` over Lightyear packets (`avg_send=101.654Mbps`) and received only `7110.6KiB` (`avg_recv=2.644Mbps`), so traffic is overwhelmingly server fanout. Fragmentation got worse (`max_fragments_per_frame=44` vs prior `32`), so the next bandwidth work should inspect which replicated components/entities dominate packet payloads instead of assuming replication cadence alone is the bottleneck.
- `tools/load_summary.py` now also reports per-process channel byte breakdowns from `channel_recv_message` and serialized `message_send` rows. Re-summarizing `logs/load/20260601-215822-local-c50` shows the two traced clients received about `97%` of decoded channel bytes from `lightyear_replication::channels::RepliconMutationsChannel` (`1901.6KiB` each), about `2.5%` from `RepliconUpdatesChannel` (`~49KiB`), about `0.2%` from `GameChannel` (`3.9KiB`), and almost nothing from ping/metadata. Client-to-server sends are mostly `SnakeInput` (`90.8KiB` and `106.8KiB` over the run), but this is only about `0.05Mbps` per traced client; server inbound is not the problem. Current limitation: the Lightyear/Replicon bridge hands opaque serialized mutation messages to the transport, so the trace can identify the mutation channel but not yet the exact component/entity (`TailPoints`, `Position`, etc.) responsible inside that channel.
- Added `config/load_no_food.ron` and reran the comparable 50-client traced benchmark with no food: `just load-local clients=50 seconds=20 release=false port=5176 trace_clients=2 server_trace=true ramp_per_second=25 net_interfaces=lo config=config/load_no_food.ron` wrote `logs/load/20260601-230426-local-c50`. Removing food did not reduce mutation traffic: traced clients received `2905.7KiB` and `2922.6KiB` from `RepliconMutationsChannel` (`99.7%` of decoded receive bytes), compared with `1901.6KiB` each in the food-heavy run. `RepliconUpdatesChannel` dropped from about `49KiB` to `5.9KiB`, which is consistent with fewer entity spawns/insertions, but mutation traffic remains dominant and even higher. The server sent `285892.9KiB` (`avg_send=106.296Mbps`) and received `7252.7KiB`; loopback averaged `98.447Mbps`; server frame time improved to `avg_frame=26.87ms`, `peak_frame=173.21ms`, and rollbacks dropped to `3` with max delta `2`. Current read: food spawn/update traffic is not the primary bandwidth source. Replicon `Mutations` are component mutations since the last acknowledged tick, so the likely next target remains frequently changed snake state such as `TailPoints`/movement components, and we need component-level byte attribution before protocol refactors.
- 2026-06-01 Chrome profiling tooling status: `cargo check -j 2 -p server --features profile-chrome`, `python3 -m py_compile tools/chrome_trace_summary.py`, and `cargo fmt --all --check` passed. A tiny `just profile-chrome clients=1 seconds=1 release=false port=5173 ramp_per_second=10 net_interfaces=lo` smoke could not complete because `/spare/ssd` was full (`408M` free, `target` about `33G`) while compiling the profiled executable, so the live trace path still needs to be rerun after freeing disk. The summarizer itself was verified against a synthetic Chrome trace.
- Moved leaderboard rank/stat presentation off direct component replication. `PlayerRank` and `PlayerStats` remain server-local components, while the server now sends room-scoped `LeaderboardSnapshot` messages every 0.5 seconds on a new sequenced-unreliable `LeaderboardChannel`. Clients cache the newest snapshot and use it for the leaderboard, minimap leader marker, and leader crown rendering. This should remove high-frequency stat churn from normal Replicon component mutation traffic while keeping `Player`, `PlayerScore`, and `PlayerStatus` replicated for gameplay/death flows. Verification: `cargo test -j 2 -p shared --lib leaderboard`, `cargo test -j 2 -p server --lib leaderboard`, `cargo test -j 2 -p client --lib leaderboard`, and `cargo check -j 2 -p server -p client`.
- 2026-06-02 switched Lightrider/Lightyear to the local Replicon checkout at `/spare/ssd/cbournhonesque/src/other/bevy_replicon` and integrated Replicon's new op-delta replication path for snake tails. Lightyear now has op-delta-aware prediction and interpolation marker writers so `TailPoints` ops reconstruct confirmed history instead of bypassing rollback/interpolation history. Lightrider registers `TailPoints` with `replicate_op_delta`, records movement/turn/shorten operations through `OpDeltaLog<TailPoints>`, and keeps direct tail mutation helpers for tests and non-ECS callers. Verification: `cargo check -j 2 -p shared`, `cargo check -j 2 -p server -p client`, `cargo test -j 2 -p shared --lib movement`, `cargo test -j 2 -p shared --lib authoritative_snake_tail_records_op_delta_mutations`, and Replicon `cargo test -j 2 --test op_delta`.
- 2026-06-02 50-client no-food op-delta benchmark: reran the comparable command `just load-local clients=50 seconds=20 release=false port=5178 trace_clients=2 server_trace=true ramp_per_second=25 net_interfaces=lo config=config/load_no_food.ron`, writing `logs/load/20260602-145629-local-c50` before that generated directory was later removed to recover from `/spare/ssd` filling up. Compared with pre-op-delta `logs/load/20260601-230426-local-c50`, loopback bandwidth fell from `98.447Mbps` avg / `180.834Mbps` peak to `19.833Mbps` avg / `28.176Mbps` peak; traced server packet send fell from `285892.9KiB` / `106.296Mbps` to `42259.9KiB` / `15.729Mbps`; traced client receive fell from about `5.7MiB` / `2.12Mbps` to about `851KiB` / `0.317Mbps`; max fragments/frame fell from `30` to `2`; traced client `RepliconMutationsChannel` bytes fell from about `2.9MiB` each to about `744KiB` each. Correctness was not acceptable yet: all 50 clients logged `cannot deserialize op-delta ops into TailPoints` and rollbacks rose from `3` to `217`, mostly `TailPoints`. A first Replicon consume-path fix compiled and passed `cargo test -j 2 --test op_delta`, but the load logs showed op-delta `Ops` were still reaching a snapshot-only write path.
- Fixed TailPoints op-delta receive-function routing. Lightrider now registers `TailPoints` custom interpolation op-delta handling before installing the interpolation function. Lightyear's interpolated marker registration now distinguishes normal insert-if-missing registration from op-delta replace-existing registration, so a later op-delta registration can replace a previously installed normal interpolation writer. Local Replicon now permits marker receive functions to be overwritten in debug builds as well as release builds, and keeps the op-delta consume function for draining stale snapshot/ops payloads. Verification: Replicon `cargo test -j 2 marker_fns_can_be_overwritten`, Lightyear `cargo test -j 2 -p lightyear_tests --lib op_delta`, Lightrider `cargo check -j 2 -p server -p client`, and final no-food smoke `just load-local clients=5 seconds=5 release=false port=5180 trace_clients=1 server_trace=true ramp_per_second=25 net_interfaces=lo config=config/load_no_food.ron` wrote `logs/load/20260602-152216-local-c5` with no op-delta deserialization errors, no panics, no packet fragments, and one 2-tick `TailPoints` rollback. The full 50-client no-food benchmark should be rerun after disk pressure is reduced.
- 2026-06-02 reran the 50-client no-food op-delta benchmark after the latest Lightyear changes using `CARGO_TARGET_DIR=/spare/local/cbournhonesque/cargo-target` to keep Cargo artifacts off `/spare/ssd`. A partial cache merge copied the Lightrider target plus most of the Lightyear target into the new shared target dir (`~72G` after builds). Verification passed: `CARGO_TARGET_DIR=/spare/local/cbournhonesque/cargo-target cargo test -j 2 -p lightyear_tests --lib op_delta`, `CARGO_TARGET_DIR=/spare/local/cbournhonesque/cargo-target cargo check -j 2 -p server -p client`, and `CARGO_TARGET_DIR=/spare/local/cbournhonesque/cargo-target just load-local clients=50 seconds=20 release=false port=5181 trace_clients=2 server_trace=true ramp_per_second=25 net_interfaces=lo config=config/load_no_food.ron`. The run wrote `logs/load/20260602-162838-local-c50` (`868M`) with no op-delta deserialization errors and no panics. Compared with the pre-op-delta no-food run, loopback averaged `21.046Mbps` vs `98.447Mbps`; traced server send averaged `17.268Mbps` vs `106.296Mbps`; traced client receive was about `0.347Mbps` each vs about `2.12Mbps`; max fragments/frame was `2` vs `30`. Correctness is much better than the previous bad op-delta run: traced rollbacks were `2` total, deltas `1` and `2`, both `TailPoints`. The raw logs still had two known duplicate-signature errors on one client where the duplicate hash mapped to the same entity; keep that as a low-priority Replicon/signature issue.
- 2026-06-03 enabled and tested Lightyear's new LZ4 transport compression. Lightrider now exposes `network.compression` in RON (`Disabled` by default, `Lz4` for compressed runs), enables Lightyear's `compression_lz4` feature, and applies the selected compression to client/server transports. Added `config/load_no_food_lz4.ron` for the comparable no-food benchmark. Verification passed: `CARGO_TARGET_DIR=/spare/local/cbournhonesque/cargo-target cargo fmt --all --check`, `CARGO_TARGET_DIR=/spare/local/cbournhonesque/cargo-target cargo test -j 2 -p shared --lib config`, and `CARGO_TARGET_DIR=/spare/local/cbournhonesque/cargo-target cargo check -j 2 -p server -p client`. The 50-client LZ4 run `CARGO_TARGET_DIR=/spare/local/cbournhonesque/cargo-target just load-local clients=50 seconds=20 release=false port=5182 trace_clients=2 server_trace=true ramp_per_second=25 net_interfaces=lo config=config/load_no_food_lz4.ron` wrote `logs/load/20260603-091326-local-c50` with no op-delta deserialization errors, panics, or despawn errors. Compared with the previous no-LZ4 op-delta run (`logs/load/20260602-162838-local-c50`), loopback bandwidth changed from `21.046Mbps` avg / `29.778Mbps` peak to `20.236Mbps` avg / `27.983Mbps` peak; traced server send changed from `17.268Mbps` to `16.912Mbps`; traced client receive changed from about `0.347Mbps` to `0.341Mbps`; traced client packet receive bytes fell from about `934.7KiB` to about `732KiB`; fragments/client fell from `82` to `64`, with max fragments/frame still `2`; server CPU/frame improved in this run (`139.5%`/`22.61ms` to `101.6%`/`17.11ms`), though that part may include run-to-run variability. Correctness remained good: `2` traced rollbacks, both 1 tick. Caveat: the channel byte breakdown is decoded post-decompression, so packet/interface bytes are the meaningful compression comparison; current logs do not emit explicit compression ratio rows yet. The first summary write hit transient `/spare/ssd` disk pressure, then `just load-summary logs/load/20260603-091326-local-c50` recovered `load_summary.txt`.
- 2026-06-03 removed the generated `logs/` directory to recover `/spare/ssd` space; benchmark numbers above are preserved in this document, but those raw run directories are intentionally gone. Historical pre-always-compress LZ4 hypothesis after reading Lightyear/Replicon code: the small win was not surprising because the packet builder only compressed final single-message packets when the uncompressed packet was over MTU, while already-fitting packets finalized uncompressed. This packet-builder detail is superseded by the next bullet. Replicon mutation messages are still compact postcard data laid out by entity/chunk (`entity`, byte-size, component payloads) rather than a columnar stream grouped by component/op field, so LZ4 may see small mixed records instead of long homogeneous arrays. If we want a real compression diagnosis, add trace/summary rows for attempted/compressed/skipped packets, original/compressed bytes, and fragment-message compression, then consider a more component/op-columnar replication format for hot streams like tail ops.
- 2026-06-03 reran the compression benchmark after Lightyear was changed to always try compressing packets, not only packets over MTU. First restored Lightyear's op-delta marker helpers against local Replicon and fixed the op-delta prediction/interpolation writers to bootstrap from the live component when history is missing, while only advancing `OpDeltaReceiver` after a base exists. Initial paired 50-client runs built into the shared target directory but executed stale binaries from `target/`, so their "no compressed packets" conclusion is superseded by the follow-up below.
- 2026-06-03 compression follow-up: added Lightyear packet compression outcome tracing plus `tools/load_summary.py` compression summaries, and fixed `just load-local` to execute binaries from `${CARGO_TARGET_DIR:-target}`. Corrected short no-food control runs show compression is active. LZ4 run `logs/load/20260603-171743-local-c10`: server `packet_send` rows had `compression_enabled=true`, `Some(Lz4)`, with `776` compressed packets and `1320` too-small packets; server send was `533.1KiB`/`0.522Mbps`; compressed packet bytes went from `692.1KiB` original to `495.4KiB`, saving `196.7KiB`. Matching no-LZ4 run `logs/load/20260603-171835-local-c10`: compression outcome was `disabled` for all packets and server send was `735.6KiB`/`0.719Mbps`. Conclusion: the absence of compressed packet rows was a load-command bug, not a Lightyear compression failure. Rerun the 50-client no-food benchmark with the fixed command before making final bandwidth claims.
- 2026-06-03 fixed-command 50-client LZ4 no-food smoke: `CARGO_TARGET_DIR=/spare/local/cbournhonesque/cargo-target just load-local clients=50 seconds=8 release=false port=5196 trace_clients=1 server_trace=true ramp_per_second=50 net_interfaces=lo config=config/load_no_food_lz4.ron` wrote `logs/load/20260603-173958-local-c50`. Server compression rows were `14975` compressed packets and `5360` too-small packets. By packet count, too-small rows are visible (`26.4%` of compression rows), but by bytes they are negligible: `166.9KiB` tiny packets vs `16153.9KiB` compressed packet originals. Compressed server packets saved `4982.0KiB` (`16153.9KiB` original to `11171.9KiB` compressed). The tiny server packets average about `32B` and occur on non-replication ticks, roughly one per active link; they are likely Lightyear sync/control packets such as ping/pong and acks. Large replication packets arrive in bursts every 10 Hz replication send, often `200-350` large packets in a single server tick for 50 clients.
